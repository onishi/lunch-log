# ランチログ (Lunch Log) 仕様書

毎日のランチを写真とともに記録し、Web で振り返り、スプレッドシートやブログへ書き出せるサービス。

- 文書バージョン: 0.2 (ドラフト)
- 最終更新: 2026-09-12
- ステータス: 仕様検討中 / 未実装

---

## 1. 背景と目的

### 1.1 解決したい課題

- 毎日ランチを食べているが、「先週どこで何を食べたか」を思い出せない。
- 写真はカメラロールに溜まるが、店名・メニュー名と紐づかず検索できない。
- 記録アプリは入力項目が多くて続かない。**入力の手間が最大の敵**。
- 食べ歩きの記録を自分の資産 (スプレッドシート / ブログ) として残したいが、
  既存サービスに閉じ込められたくない。

### 1.2 プロダクトの狙い

1. **3 タップで記録が終わる**こと。撮る → 候補から店を選ぶ → 保存。
2. 位置情報・EXIF・メニュー写真の OCR から、**人間が打つ文字を極小化**する。
3. 記録したデータは**いつでも外に出せる** (Sheets / ブログ / JSON エクスポート)。

### 1.3 非目標 (スコープ外)

- グルメ SNS・レビュー投稿プラットフォーム (他人のレビューを読む機能)。
- 店舗予約、クーポン、決済。
- ランチ以外の食事の網羅的な記録 (夕食・朝食も保存自体は可能だが最適化しない)。
- カロリー・栄養素の自動算出 (将来検討)。
- **収益化** — 課金・広告・アフィリエイトは一切行わない。本サービスは
  作者自身と少数の利用者のための非商用ツールとして設計する。
- 食べログのコンテンツ (点数・レビュー・店舗詳細) の取り込み。URL の保存のみ行う (§6.3)。

---

## 2. ターゲットとユースケース

### 2.1 想定ユーザー

- オフィスワーカー / リモートワーカーで、平日ランチを外食・テイクアウトする人。
- 自分のライフログを残したい、ブログやスプレッドシートを使い慣れた層。
- Android + Google アカウント利用者 (初期は Android のみ)。

### 2.2 主要ユースケース

| ID | ユースケース | 頻度 |
|----|--------------|------|
| UC-1 | 着席後にメニュー表を撮影 → メニュー名の候補を得る | 毎回 |
| UC-2 | 料理が来たら撮影 → 位置情報から店が自動で埋まる → 保存 | 毎日 |
| UC-3 | 後から (帰社後・帰宅後) 写真を選んでまとめて記録する | 週数回 |
| UC-4 | 食べログの URL を共有シートからアプリに送り、店舗へのリンクとして残す | 時々 |
| UC-5 | Web でカレンダー / 一覧を眺め、先月の記録を振り返る | 週 1 |
| UC-6 | 月末にスプレッドシートへ同期して経費・支出を集計する | 月 1 |
| UC-7 | 気に入った店をブログ記事として下書き投稿する | 月数回 |

### 2.3 代表的な体験フロー (UC-2)

```
[ホーム] --(FABタップ)--> [カメラ]
   撮影 (料理写真)
        |
        v
[記録編集画面]  ← 位置情報から近隣店舗を取得し「店名」を候補表示
   店名: ◯◯食堂 (自動、ワンタップで確定)
   メニュー: (メニュー写真OCR/過去履歴から候補)
   金額: (任意)
        |
    [保存] ← ここまで平均 15 秒 / タップ 3 回以内
        v
[ホーム] 本日のカードが追加される
```

---

## 3. 機能要件

優先度: **P0** = MVP 必須 / **P1** = 初期リリース時に欲しい / **P2** = 将来。

### 3.1 記録 (Android)

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-101 | 料理写真の撮影・添付 | P0 | CameraX による撮影、またはギャラリーから選択。1 記録あたり最大 10 枚。 |
| F-102 | メニュー写真の撮影・添付 | P0 | 写真に `種別 = メニュー表` を付与。料理写真と区別して保存・表示。 |
| F-103 | メニュー名の入力 | P0 | 自由入力 + 候補サジェスト (自分の過去入力 / 同店の他記録 / OCR 結果)。 |
| F-104 | 店名の入力 | P0 | 自由入力 + 位置情報からの候補サジェスト。 |
| F-105 | 位置情報の取得と店舗補完 | P0 | 端末 GPS または写真 EXIF の GPS から近隣飲食店を検索し候補提示。 |
| F-106 | 日時の記録 | P0 | 既定は写真の撮影日時 (EXIF)、無ければ保存時刻。手動変更可。 |
| F-107 | 金額の記録 | P1 | 数値入力。通貨は JPY 固定 (将来多通貨)。 |
| F-108 | 評価・メモ・タグ | P1 | 5 段階評価、自由メモ、タグ (#ラーメン #新規開拓 など)。 |
| F-109 | 食べログ URL の保存 | P1 | URL を貼付 / 共有インテント受信で店舗に紐づけて保存。取得・解析はしない (§6.3)。 |
| F-110 | メニュー写真の OCR | P1 | ML Kit Text Recognition (日本語) でメニュー候補を抽出しチップ表示。 |
| F-111 | 下書き / オフライン保存 | P0 | 圏外でもローカルに保存し、オンライン復帰時に自動同期。 |
| F-112 | 記録の編集・削除 | P0 | 削除は論理削除 (30 日間はゴミ箱から復元可)。 |
| F-113 | 共有インテント受信 | P1 | 他アプリ (カメラ・ブラウザ) からの画像 / URL 共有でアプリを起動。 |
| F-114 | ホーム画面ウィジェット / クイックタイル | P2 | ワンタップで撮影記録を開始。 |
| F-115 | リマインダー通知 | P2 | 平日 13:00 に「今日のランチ記録しませんか」(ユーザー設定で ON/OFF)。 |

### 3.2 閲覧・検索 (Android / Web 共通)

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-201 | タイムライン表示 | P0 | 日付降順のカード一覧。無限スクロール。 |
| F-202 | 写真グリッド表示 | P1 | 料理写真だけを格子状に表示。 |
| F-203 | カレンダー表示 | P1 | 月カレンダー上に各日のサムネイル。 |
| F-204 | 記録詳細 | P0 | 写真スライド、店名、メニュー、金額、評価、メモ、地図。 |
| F-205 | 検索 | P1 | 店名 / メニュー名 / メモ / タグの部分一致検索。 |
| F-206 | フィルタ | P1 | 期間、評価、タグ、店舗、金額レンジ。 |
| F-207 | 店舗別まとめ | P1 | 「この店で食べたもの」一覧と訪問回数。 |
| F-208 | 統計ダッシュボード | P2 | 月間支出、訪問店舗数、ジャンル比率、リピート率。 |
| F-209 | 地図表示 | P2 | 記録をピンで地図に配置。 |

### 3.3 アカウント・同期

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-301 | Google ログイン | P0 | Firebase Authentication (Google プロバイダ) で Android / Web 共通ログイン。 |
| F-302 | 複数端末同期 | P0 | 同一アカウントの Android と Web で同じデータを参照。 |
| F-303 | ログアウト / アカウント削除 | P0 | 削除時は全データと画像を 30 日以内に完全消去。 |
| F-304 | 匿名利用 → 後からログイン | P2 | 匿名アカウントで開始し、ログイン時にデータを引き継ぐ。 |

### 3.4 エクスポート・連携

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-401 | Google スプレッドシート出力 | P1 | 記録を 1 行 1 レコードで追記。初回にシートを自動生成。**出力の一方向のみ** (§6.4)。 |
| F-402 | スプレッドシート自動出力 | P2 | 新規記録を日次バッチで自動追記。 |
| F-403 | はてなブログ投稿 | P1 | はてなブログへ AtomPub で下書き投稿 (§6.5)。他ブログサービスは対象外。 |
| F-404 | Markdown / HTML 生成 | P1 | 日次・月次まとめを Markdown で生成しコピー / 共有。 |
| F-405 | CSV / JSON エクスポート | P1 | 全データのダウンロード (写真は URL または ZIP)。 |
| F-406 | 公開ページ | P2 | 記録を限定公開 URL で共有 (個別 / 月次)。 |

---

## 4. 非機能要件

| 分類 | 要件 |
|------|------|
| 性能 | アプリ起動からカメラ表示まで 2 秒以内。保存操作のローカル完了は 300ms 以内 (アップロードは非同期)。 |
| オフライン | 記録の作成・閲覧・編集はオフラインで可能。同期は WorkManager による再試行付きキュー。 |
| 通信量 | 写真は端末側で長辺 2048px / JPEG 品質 85 にリサイズしてからアップロード (原本保持はオプション)。 |
| 対応 OS | Android 10 (API 29) 以上。ターゲット SDK は最新安定版。 |
| Web | Chrome / Safari / Edge の最新 2 バージョン。モバイルブラウザ対応 (レスポンシブ)。 |
| 可用性 | マネージドサービス依存。SLA はプラットフォーム準拠。個人利用規模を想定。 |
| セキュリティ | 全通信 HTTPS。Firestore / Storage はセキュリティルールで所有者のみアクセス可。 |
| プライバシー | 位置情報は任意。EXIF 位置情報の除去オプション。既定は完全非公開。 |
| 国際化 | 文言は日本語を第一言語とし、リソース分離して英語追加可能な構造にする。 |
| アクセシビリティ | コントラスト比 4.5:1 以上、TalkBack 対応のコンテンツ説明。 |
| コスト | 非商用ツールとして、数人規模 / 1 人あたり月 20 記録で各サービスの無料枠に収まることを目標とする。超過しやすいのは Places API と Cloud Storage (§13)。 |
| 監視 | アプリ・Functions の稼働状況とエラーを自作監視基盤 **monotir** に集約する (§6.6)。外部 APM は使わない。 |

---

## 5. システム構成

### 5.1 全体像

```
  ┌────────────────┐        ┌────────────────┐
  │ Android アプリ  │        │  Web (Next.js) │
  │ Kotlin/Compose │        │   React/SSR    │
  └───────┬────────┘        └───────┬────────┘
          │  Firebase SDK           │
          ├─────────────┬───────────┤
          v             v           v
  ┌────────────┐ ┌────────────┐ ┌──────────────┐
  │  Firebase  │ │ Firestore  │ │   Firebase   │
  │    Auth    │ │    (DB)    │ │   Storage    │
  └────────────┘ └─────┬──────┘ └──────────────┘
                       │ トリガ
                       v
              ┌──────────────────┐    ┌──────────────────┐
              │ Cloud Functions  │───>│ Google Sheets    │ (出力のみ)
              │  / Cloud Run     │───>│ はてなブログ      │ (AtomPub/下書き)
              │                  │───>│ Places API       │ (店舗補完)
              └────────┬─────────┘    └──────────────────┘
                       │
                       │ イベント / エラー / 所要時間
       Android ────────┼────────> ┌──────────────────┐
       Web ────────────┘          │ monotir (自作監視) │
                                  └──────────────────┘
```

### 5.2 技術選定

| レイヤ | 採用 | 理由 |
|--------|------|------|
| Android | Kotlin / Jetpack Compose / MVVM + UseCase | 最新の標準構成。宣言的 UI で開発速度が出る。 |
| ローカル DB | Room | オフラインファーストの Single Source of Truth。 |
| 同期 | WorkManager + Firestore オフライン永続化 | プロセス終了後も再試行される。 |
| カメラ | CameraX | 端末差異の吸収。 |
| 画像読み込み | Coil | Compose 親和性。 |
| OCR | ML Kit Text Recognition v2 (日本語) | オンデバイスで無料・オフライン可。 |
| 認証 | Firebase Authentication (Google) | Android / Web 共通、Credential Manager 対応。 |
| DB | Cloud Firestore | リアルタイム同期とオフラインキャッシュが標準装備。 |
| ファイル | Cloud Storage for Firebase | 画像保管。セキュリティルールで保護。 |
| サーバ処理 | Cloud Functions (TypeScript) | 外部 API 呼び出しと秘密鍵の隠蔽。 |
| Web | Next.js (App Router) + Firebase Hosting | SSR/ISR による高速表示と OGP 対応。 |
| 地図・店舗 | Google Places API (New) / Maps SDK | 位置情報からの店舗補完。 |
| 監視 | **monotir** (自作監視基盤) | アプリ / Functions のイベント・エラー・所要時間を自前で集約 (§6.6)。外部 SaaS に依存せず、データを手元に置ける。 |

> **代替案:** Firestore ではなく Cloud Run + PostgreSQL(PostGIS) にすると、
> 複雑な集計・地理検索・全文検索が容易になる。将来、統計機能 (F-208) が
> 主要価値になる場合は移行を検討する。MVP は開発速度を優先し Firebase を採用。

---

## 6. 外部連携仕様

### 6.1 位置情報による店舗補完 (F-105)

**入力**
1. 端末の現在地 (FusedLocationProvider、精度優先 / 10 秒タイムアウト)。
2. 写真 EXIF の GPS タグ (後から登録する場合に使用)。

**処理**
1. 取得した緯度経度で Places API `searchNearby` を呼ぶ (Cloud Functions 経由)。
   - `includedPrimaryTypes: ["restaurant", "cafe", "bakery", "meal_takeaway"]`
   - 半径 150m (取得できなければ 300m まで自動拡大)
   - `rankPreference: DISTANCE`、最大 10 件
2. 以下でスコアリングし並べ替える。
   - 距離 (近いほど高)
   - ユーザーの過去訪問履歴 (訪問済みは大きく加点)
   - 記録時刻が営業時間内か
3. 上位 5 件を候補チップとして表示。1 タップで店名・住所・placeId・座標を確定。

**フォールバック**
- 位置情報権限が無い / 取得失敗 → 直近に訪れた店の履歴を候補として表示。
- どれにも当たらない → 自由入力 (新規店舗として登録)。

**プライバシー**
- 位置情報権限は「記録時のみ (WHEN_IN_USE)」。バックグラウンド追跡はしない。
- 権限拒否でもアプリの全機能が手動入力で利用できること。

### 6.2 メニュー写真 OCR (F-110)

1. メニュー写真を ML Kit でオンデバイス OCR。
2. テキストブロックを行単位に整形し、次のルールでメニュー候補を抽出。
   - 価格らしき数値 (`¥?[0-9,]{3,5}円?`) を含む行 → その行の非数値部分をメニュー名候補、数値を価格候補とする。
   - 1〜30 文字程度の行を候補に含める。
3. 候補をチップで提示。タップでメニュー名 / 金額に反映。
4. OCR 全文は `photos.ocrText` に保存し、検索対象に含める (P2)。

### 6.3 食べログ URL の保存 (F-109)

**方針: URL を保存するだけ。食べログへのアクセス・解析は一切行わない。**

食べログは第三者向けの公開 API を提供しておらず、ページの取得・解析は
利用規約や robots.txt に抵触しうる。本サービスはスクレイピングも
メタデータ取得も行わず、**ユーザーが貼り付けた URL をそのまま保管する**
だけに留める。これにより法務リスクを完全に回避し、外部サイトの構造変更に
よる故障も起きない。

**仕様**

1. 記録編集画面と店舗編集画面に「食べログ URL」欄を置く。
2. 入力方法は 2 つ。
   - 手動でペースト。
   - ブラウザの共有メニューから URL をアプリへ送る (`ACTION_SEND` / `text/plain`)。
     `tabelog.com` ドメインの URL を受け取ったら、新規記録の下書きを開き
     URL 欄を埋めた状態で表示する。
3. 保存先は `restaurants.tabelogUrl` (店舗に 1 つ)。記録側からは店舗経由で参照する。
4. バリデーションはクライアント内で完結させる。
   - スキームが `https`、ホストが `tabelog.com` またはそのサブドメインであること。
   - クエリパラメータ (トラッキング ID 等) は除去して正規化する。
   - 条件を満たさない URL は「食べログの URL ではないようです」と警告するが、
     保存自体は許可する (ユーザーの判断を尊重)。
5. 表示は**リンクのみ**。記録詳細・店舗詳細に「食べログで見る ↗」ボタンを置き、
   タップで外部ブラウザ / カスタムタブを開く。ページ内容の埋め込みやプレビューはしない。

**店名・住所の補完はどこから来るか**

食べログからは取らないため、店舗情報の自動補完は以下に一本化する。

- 位置情報からの Places API `searchNearby` (§6.1) — 主経路。
- 店名テキストからの Places API `searchText` — ユーザーが店名を打った場合の補完。
- 上記で見つからなければ手入力。

つまり「食べログ URL は店舗への外部リンクとして保存されるだけで、
入力補助の情報源ではない」。URL を貼っても店名欄は自動で埋まらない。

### 6.4 Google スプレッドシート出力 (F-401)

> **方向は一方向 (アプリ → シート) のみ。** シート側での編集をアプリへ取り込む
> 双方向同期は行わない。シートは「持ち出し先・集計用のビュー」であり、
> 正本は常に Firestore のデータとする。

**認可**
- Firebase の Google ログインとは別に、**増分認可 (incremental auth)** で
  `https://www.googleapis.com/auth/drive.file` スコープを追加取得する。
  - `drive.file` はアプリが作成したファイルのみアクセス可能で、ユーザーの
    既存 Drive 全体を読まないため、プライバシー上最も望ましい。
- リフレッシュトークンは Cloud Functions 側で暗号化して保管 (Secret Manager / KMS)。

**シート構成**
- 初回連携時に `ランチログ` という名前のスプレッドシートを新規作成。
- シート名は `records`。1 行目はヘッダ固定。

| 列 | 内容 | 例 |
|----|------|-----|
| A | 記録ID | `rec_01HX...` |
| B | 日付 | `2026-09-12` |
| C | 時刻 | `12:35` |
| D | 店名 | `◯◯食堂` |
| E | メニュー名 | `日替わり定食` |
| F | 金額 | `980` |
| G | 評価 | `4` |
| H | タグ | `定食,リピート` |
| I | メモ | `味噌汁がうまい` |
| J | 住所 | `東京都渋谷区…` |
| K | 緯度 | `35.6581` |
| L | 経度 | `139.7016` |
| M | 食べログURL | `https://tabelog.com/…` |
| N | 料理写真URL | (署名付き or 公開 URL) |
| O | メニュー写真URL | |
| P | 最終更新 | ISO8601 |

**出力方式**
- 手動出力: 「スプレッドシートに書き出す」ボタンで未出力分を書き込む。
- 差分管理: A 列の記録 ID をキーに、既存行があれば `values.update` で上書き、
  無ければ `values.append` で追記する (upsert)。
- 自動出力 (F-402): 日次バッチ (Cloud Scheduler → Functions) で前日分をまとめて反映。
- ユーザーがシートを手で編集していた場合、アプリが書き込む A〜P 列は
  次回出力時に上書きされる。**Q 列以降はアプリが触らない**ため、
  ユーザーの追加列 (自作の集計式やメモ) は保持される。この仕様を設定画面に明記する。
- シートの削除・破損時は「シートを作り直す」操作で再生成し、全件を再出力する。

### 6.5 はてなブログ連携 (F-403)

**対象ははてなブログのみ。** WordPress / note / Micropub などは対象外とする
(将来必要になれば §6.5 を拡張する)。連携先を持たないユーザー向けには
Markdown 書き出し (F-404) をフォールバックとして提供する。

**認証**

- 設定画面で **はてな ID** と **API キー** (はてなの「アカウント設定 → AtomPub」で発行)
  を登録する。ブログ ID (`example.hatenablog.com`) も入力する。
- AtomPub は WSSE 認証または Basic 認証に対応する。**Basic (HTTPS 上)** を採用し、
  実装を単純に保つ。
- 認証情報は Cloud Functions が Secret Manager に保存し、**クライアントへは返さない**。
  設定画面には「登録済み / 未登録」の状態だけを表示する。
- 登録時に `GET /{hatena-id}/{blog-id}/atom/entry` を 1 回叩いて疎通確認を行い、
  失敗したら保存しない。

**投稿**

- エンドポイント: `POST https://blog.hatena.ne.jp/{hatena-id}/{blog-id}/atom/entry`
- ボディは Atom XML:

```xml
<?xml version="1.0" encoding="utf-8"?>
<entry xmlns="http://www.w3.org/2005/Atom"
       xmlns:app="http://www.w3.org/2007/app">
  <title>2026-09-12 のランチ</title>
  <author><name>{hatena-id}</name></author>
  <content type="text/x-markdown">{{本文}}</content>
  <updated>2026-09-12T12:35:00+09:00</updated>
  <category term="ランチ" />
  <category term="◯◯食堂" />
  <app:control>
    <app:draft>yes</app:draft>
  </app:control>
</entry>
```

- **常に下書き (`<app:draft>yes</app:draft>`) で投稿する。** 公開はユーザーが
  はてなブログ側で行う。誤爆を防ぐためこの既定は変更できない設定とする
  (将来「公開で投稿」を足す場合は明示的なオプトインにする)。
- 投稿成功時、レスポンスの `<link rel="edit">` と `<link rel="alternate">` を
  `records.sync.blog` に保存し、記録詳細から編集画面へ飛べるようにする。
- 再投稿は `PUT {edit-url}` で同じ記事を更新する (記事の重複を作らない)。

**写真の扱い**

- はてなフォトライフへの直接アップロードは行わず、**Cloud Storage の
  署名付き URL (有効期限なしの公開読み取り URL)** を本文から参照する。
  - 理由: フォトライフ API の追加実装を避けるため。
  - この方式では、ブログ記事の画像は Cloud Storage のファイルに依存する。
    アカウント削除時に画像が消えて記事のリンクが切れることを設定画面で明記する。
- 記事に含める写真は「料理写真の 1 枚目」を既定とし、編集画面で追加・変更できる。

**記事テンプレート (ユーザー編集可能)**

```markdown
# {{date}} のランチ

## {{restaurantName}} / {{dishName}}

![{{dishName}}]({{dishPhotoUrl}})

- 店名: {{restaurantName}}{{#tabelogUrl}} ([食べログ]({{tabelogUrl}})){{/tabelogUrl}}
- メニュー: {{dishName}}
- 金額: {{price}}円
- 評価: {{stars}}

{{memo}}
```

- 投稿単位は「1 記録 = 1 記事」「1 日 = 1 記事」「1 ヶ月 = 1 記事」から選択可能。
- テンプレートは Mustache 風の記法とし、設定画面で編集できる。
  未設定の項目 (金額・評価など) の行は自動的に省かれる。

### 6.6 monotir 連携 (監視)

> **前提の確認が必要:** monotir は利用者の自作監視基盤であり、本節は
> 「HTTP で JSON のイベントを受け取るエンドポイントを持つ」という仮定で
> 記述している。実際のプロトコル (OpenTelemetry OTLP / 独自 JSON / ログ転送)、
> 認証方式、メトリクスのモデル (イベント指向かメトリクス指向か) が判明したら
> 本節を書き換えること。§14 の未決事項 M-1 を参照。

**目的**

- 外部 APM (Crashlytics / Sentry など) に依存せず、稼働状況を自前で把握する。
- 非商用ツールのため、監視は「壊れたことに気づける」最小限で十分とする。

**送信するもの**

| 種別 | 例 | 送信元 |
|------|-----|--------|
| エラー | 未捕捉例外、同期失敗、外部 API エラー | Android / Web / Functions |
| 所要時間 | `time_to_save_ms` (カメラ起動〜保存完了)、Places API 応答時間 | Android / Functions |
| カウンタ | 記録作成数、シート出力件数、ブログ投稿数、店舗候補の採用/棄却 | Android / Functions |
| ヘルスチェック | Functions の死活、日次バッチの成否 | Cloud Scheduler → Functions → monotir |

**送信形式 (仮)**

```jsonc
POST {MONOTIR_ENDPOINT}/v1/events
Authorization: Bearer {MONOTIR_TOKEN}

{
  "service": "lunch-log",
  "component": "android",        // android | web | functions
  "kind": "error",               // error | duration | counter | health
  "name": "sheets_sync_failed",
  "value": 1,                    // duration ならミリ秒
  "at": "2026-09-12T03:35:00Z",
  "attributes": {
    "version": "1.2.0",
    "os": "Android 14",
    "errorCode": "SHEETS_QUOTA_EXCEEDED"
  }
}
```

**設計上の約束**

- **個人を特定できる情報を送らない。** `uid` は送信せず、必要なら
  ソルト付きハッシュの先頭 8 文字のみを使う。店名・メニュー名・写真・
  位置情報は一切送らない。
- **監視の失敗がアプリの失敗にならない。** 送信は fire-and-forget。
  タイムアウト 3 秒、失敗しても握り潰し、リトライは次回起動時のバッチのみ。
- Android では端末内キューに貯め、Wi-Fi 接続時に WorkManager でまとめて送る
  (通信量とバッテリーの配慮)。
- monotir が落ちていてもアプリは通常どおり動作すること。エンドポイント未設定時は
  送信処理を完全に無効化する (開発ビルド / OSS 配布時を想定)。

**残す標準機能**

- Android のクラッシュは Google Play Console の「Android vitals」で自動収集される
  ものを併用する (追加実装不要)。monotir へはアプリ内で捕捉できた例外を送る。
- Cloud Functions のログは Cloud Logging に残る (既定動作)。monotir へは
  そこから重要なものだけを抽出して送る。

---

## 7. データモデル

Firestore のコレクション構成 (`{}` はドキュメント ID)。

```
users/{uid}
users/{uid}/records/{recordId}
users/{uid}/restaurants/{restaurantId}   # ユーザーごとの店舗キャッシュ
users/{uid}/integrations/{provider}      # sheets / hatena / wordpress
restaurantsPublic/{placeId}              # 共有マスタ (P2)
```

### 7.1 `users/{uid}`

```jsonc
{
  "displayName": "onishi",
  "email": "user@example.com",
  "photoUrl": "https://…",
  "locale": "ja-JP",
  "timeZone": "Asia/Tokyo",
  "settings": {
    "defaultCurrency": "JPY",
    "stripExifOnUpload": true,
    "reminderEnabled": false,
    "reminderTime": "13:00",
    "autoSyncSheets": false
  },
  "createdAt": "2026-09-12T02:00:00Z",
  "updatedAt": "2026-09-12T02:00:00Z"
}
```

### 7.2 `records/{recordId}`

```jsonc
{
  "id": "rec_01HX...",              // ULID
  "eatenAt": "2026-09-12T03:35:00Z", // UTC。表示はユーザー TZ
  "dishName": "日替わり定食",
  "restaurantName": "◯◯食堂",        // 非正規化 (表示高速化のため)
  "restaurantId": "rst_01HX...",     // null 可
  "price": 980,
  "currency": "JPY",
  "rating": 4,                        // 1-5、null 可
  "memo": "味噌汁がうまい",
  "tags": ["定食", "リピート"],
  "photos": [
    {
      "id": "pho_01HX...",
      "kind": "dish",                // dish | menu | exterior | receipt
      "storagePath": "users/{uid}/photos/pho_01HX.jpg",
      "thumbPath":   "users/{uid}/thumbs/pho_01HX.jpg",
      "width": 2048, "height": 1536,
      "takenAt": "2026-09-12T03:34:00Z",
      "ocrText": null,
      "order": 0
    }
  ],
  "location": {
    "lat": 35.6581, "lng": 139.7016,
    "geohash": "xn76urx",
    "accuracy": 12.5,
    "source": "device"               // device | exif | place | manual
  },
  "source": "camera",                // camera | gallery | share | web
  "visibility": "private",           // private | unlisted
  "sync": {
    "sheets": { "syncedAt": null, "rowId": null },
    "blog":   { "postedAt": null, "url": null, "editUrl": null, "provider": "hatena" }
  },
  "createdAt": "…", "updatedAt": "…", "deletedAt": null
}
```

### 7.3 `restaurants/{restaurantId}`

```jsonc
{
  "id": "rst_01HX...",
  "name": "◯◯食堂",
  "nameKana": "マルマルショクドウ",
  "googlePlaceId": "ChIJ...",
  "tabelogUrl": "https://tabelog.com/tokyo/A1303/…/",
  "address": "東京都渋谷区…",
  "lat": 35.6581, "lng": 139.7016,
  "genres": ["定食", "和食"],
  "nearestStation": "渋谷",
  "visitCount": 7,
  "lastVisitedAt": "2026-09-12T03:35:00Z",
  "createdAt": "…", "updatedAt": "…"
}
```

### 7.4 `integrations/{provider}`

```jsonc
{
  "provider": "sheets",
  "enabled": true,
  "config": { "spreadsheetId": "1AbC…", "sheetName": "records" },
  "secretRef": "projects/…/secrets/sheets-token-{uid}/versions/latest",
  "lastSyncedAt": "2026-09-12T04:00:00Z",
  "lastError": null
}
```

### 7.5 インデックス

- `records`: `eatenAt DESC` (一覧)
- `records`: `restaurantId ASC, eatenAt DESC` (店舗別)
- `records`: `tags ARRAY, eatenAt DESC` (タグ絞り込み)
- `records`: `deletedAt ASC, eatenAt DESC` (ゴミ箱除外)
- 全文検索は MVP ではクライアント側の部分一致で対応。データ量増加時に
  Algolia または Firestore 拡張機能の導入を検討。

### 7.6 セキュリティルール (方針)

```
match /users/{uid}/{document=**} {
  allow read, write: if request.auth != null && request.auth.uid == uid;
}
```

- Storage も同様に `users/{uid}/**` を所有者のみに限定。
- 限定公開 (F-406) は Cloud Functions が発行する署名付き URL 経由のみ。

---

## 8. API 仕様 (Cloud Functions)

クライアントから直接叩けない処理のみサーバに置く。認証は Firebase ID トークン (Bearer)。

| メソッド | パス | 概要 |
|----------|------|------|
| `POST` | `/v1/places/nearby` | 緯度経度から近隣飲食店候補を返す (Places API ラッパ)。 |
| `POST` | `/v1/places/search` | テキストから店舗を検索。 |
| `POST` | `/v1/integrations/sheets/connect` | OAuth コールバック処理とシート初期化。 |
| `POST` | `/v1/integrations/sheets/export` | 未出力レコードをシートへ upsert (一方向)。 |
| `POST` | `/v1/integrations/hatena/connect` | はてな ID / API キーの疎通確認と保存。 |
| `POST` | `/v1/integrations/hatena/publish` | 記事を生成しはてなブログへ下書き投稿。 |
| `GET`  | `/v1/export?format=json\|csv` | 全記録のエクスポート。 |
| `POST` | `/v1/account/delete` | アカウントとデータの削除ジョブ登録。 |

**共通エラー形式**

```jsonc
{ "error": { "code": "PLACES_QUOTA_EXCEEDED",
             "message": "店舗候補を取得できませんでした",
             "retryable": true } }
```

- レート制限: 1 ユーザーあたり 60 req/分。超過時 `429`。
- 外部 API 障害時はクライアントを手動入力にフォールバックさせる (機能を止めない)。
- 全エンドポイントは処理の成否と所要時間を monotir へ送信する (§6.6)。

---

## 9. 画面仕様

### 9.1 Android

| 画面 | 内容 |
|------|------|
| スプラッシュ / ログイン | Google ログインボタンのみ。初回のみ表示。 |
| ホーム (タイムライン) | 日付ごとのセクション、記録カード (写真・店名・メニュー・金額)。右下に FAB (カメラ)。下部ナビ: ホーム / 検索 / 統計 / 設定。 |
| カメラ | CameraX プレビュー。シャッター、ギャラリー選択、種別切替 (料理 / メニュー)。 |
| 記録編集 | 写真サムネイル列、店名 (候補チップ)、メニュー名 (候補チップ)、金額、評価、タグ、メモ、日時、食べログ URL 欄 (リンク保存のみ)。上部に「保存」。 |
| 記録詳細 | 写真ページャ、各項目、地図サムネイル、編集 / 削除 / 共有 / ブログ投稿。 |
| 検索 | 検索窓 + フィルタチップ + 結果リスト。 |
| 店舗詳細 | 店舗情報、訪問回数、その店の記録一覧。 |
| 統計 | 月間支出、記録数、ジャンル比率、よく行く店 Top5。 |
| 設定 | アカウント、連携 (Sheets / ブログ)、通知、プライバシー (EXIF 除去)、エクスポート、ログアウト、アカウント削除。 |

**入力最小化の設計原則**
- 保存に必須なのは「写真 1 枚」または「メニュー名」のどちらか一方のみ。
- 店名・日時・位置は自動補完され、ユーザーは確認するだけ。
- 候補チップは常に「最も選ばれそうなもの」を左端に置く。

### 9.2 Web

| 画面 | 内容 |
|------|------|
| ログイン | Google ログイン。 |
| 一覧 | タイムライン / グリッド / カレンダーの 3 表示切替。 |
| 詳細 | 写真拡大、全項目表示、編集。 |
| 検索・フィルタ | 期間・タグ・店舗・評価。 |
| 統計 | 月次グラフ。 |
| 設定 | 連携設定、エクスポート。 |
| 公開ページ (P2) | 限定公開 URL。OGP 対応で SNS 共有可。 |

- Web からの新規作成も可能 (写真アップロード + 手入力)。位置補完は
  ブラウザ Geolocation または写真 EXIF を使用。

---

## 10. 権限・プライバシー・法務

| 項目 | 方針 |
|------|------|
| Android 権限 | `CAMERA` (必須)、`ACCESS_FINE_LOCATION` (任意)、`POST_NOTIFICATIONS` (任意)。ストレージは Photo Picker を使い権限不要にする。 |
| 権限説明 | 初回リクエスト前に「なぜ必要か」を画面で説明してから OS ダイアログを出す。 |
| 位置情報 | 記録時のみ取得。保存しない設定も提供。既定の公開範囲は非公開。 |
| EXIF | アップロード前に位置情報を除去する設定を既定 ON。 |
| データ所有 | ユーザーはいつでも全データをエクスポート・削除できる。 |
| 削除 | アカウント削除要求から 30 日以内に Firestore / Storage / 連携トークンを完全消去。 |
| 第三者提供 | しない。監視データは自前の monotir にのみ送り、個人特定情報は含めない (§6.6)。外部 APM / 広告 SDK は組み込まない。 |
| 外部 API 規約 | Google Maps Platform の利用規約 (キャッシュ制限・帰属表示) を遵守。食べログは URL の保存とリンク表示のみで、ページ取得・解析は行わない (§6.3)。 |
| 収益 | 非商用。課金・広告・アフィリエイトリンクを置かない。食べログ URL もアフィリエイトタグを付けない (正規化時にクエリを除去する)。 |
| 店舗写真 | メニュー表の撮影は店舗により禁止されている場合がある旨をアプリ内で注意喚起。 |
| 必要書類 | プライバシーポリシー、利用規約、Play Console のデータセーフティ申告。 |

---

## 11. 分析・品質

**計測イベント** — 送信先は monotir (§6.6)。

| イベント名 | 種別 | 用途 |
|------------|------|------|
| `record_created` | counter | 記録数。属性: source / has_photo / has_location / autofilled_restaurant |
| `time_to_save_ms` | duration | カメラ起動から保存完了まで。**最重要指標** |
| `restaurant_suggestion_accepted` / `_rejected` | counter | 店舗候補の精度 |
| `places_nearby_ms` | duration | Places API 応答時間 |
| `sheets_export_completed` / `_failed` | counter | シート出力の成否 |
| `hatena_publish_completed` / `_failed` | counter | ブログ投稿の成否 |
| `sync_queue_depth` | counter | 未同期レコード数 (オフライン同期の健全性) |
| `unhandled_error` | error | 未捕捉例外 |

**主要 KPI** (自分が使い続けられているかの確認が目的。事業指標ではない)

- 週あたりの記録数 (目標: 平日の 8 割以上)。
- 記録 1 件あたりの所要時間 (目標: 中央値 15 秒以内)。
- 店舗名の自動補完採用率 (目標: 70% 以上)。
- 同期失敗率 (目標: 1% 未満)。

**アラート** (monotir 側で設定)

- 日次バッチが 2 日連続で失敗。
- `unhandled_error` が 1 日 10 件を超える。
- `sheets_export_failed` / `hatena_publish_failed` の連続発生。

**テスト**
- ユニット: ViewModel / UseCase / OCR 解析ロジック / スプレッドシート行変換 / 食べログ URL の検証と正規化 / Atom XML 生成。
- 結合: Firestore エミュレータ + Functions エミュレータ。
- UI: Compose UI Test で「撮影 → 保存」のハッピーパス。
- 手動: 圏外 → 復帰時の同期、権限拒否時の動作、大量データ (1,000 件) のスクロール。

---

## 12. 開発マイルストーン

| フェーズ | 内容 | 完了条件 |
|----------|------|----------|
| M0: 設計 | 本仕様の確定、Firebase プロジェクト作成、CI 整備、monotir の連携方式確定 (§14 M-1) | 仕様レビュー完了 |
| M1: MVP | F-101〜F-106, F-109, F-111, F-112, F-201, F-204, F-301, F-302 | 自分で毎日使える |
| M2: 補完強化 | F-105 高度化, F-110, F-107, F-108, F-205, F-206 | 入力が 15 秒以内 |
| M3: Web | Web 閲覧一式 (F-201〜F-207) | PC から振り返れる |
| M4: 連携 | F-401, F-403, F-404, F-405 | シート / ブログへ出せる |
| M5: 拡張 | F-208, F-209, F-402, F-406, ウィジェット | — |

---

## 13. リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| 食べログから店舗情報を自動取得できない | 店名・住所の手入力が残る | URL 保存のみと割り切った上で、Places API による補完 (§6.1) の精度向上に投資する。`restaurant_suggestion_accepted` を監視し、採用率が 70% を下回るならスコアリングを見直す。 |
| Places API のコスト増 | 運用費 (非商用なので自腹) | 呼び出しをサーバ集約し、同一座標 (100m グリッド) の結果を 24 時間キャッシュ。セッショントークンを活用。GCP に予算アラートを設定し、上限超過時は候補提示を無効化して手入力に落とす。 |
| 写真によるストレージ肥大 | 費用 | アップロード前リサイズ、サムネイル生成、原本保持はオプトイン。 |
| 入力が面倒で継続されない | 離脱 | 保存必須項目を 1 つに絞る。`time_to_save_ms` を継続監視。 |
| OCR 精度が低い | 期待外れ | あくまで「候補提示」に留め、手入力を常に一級市民として扱う。 |
| Firestore の集計・検索が弱い | 機能制約 | 統計は事前集計ドキュメントを Functions で更新。必要なら PostgreSQL へ移行。 |
| はてな API キーの漏洩 | 重大 (ブログを勝手に投稿される) | Secret Manager 管理、クライアントへ返さない、失効時は再認証を促す。投稿は常に下書きに限定し、被害範囲を下書き汚染に留める。 |
| monotir 自体が落ちる / 自作ゆえの運用負荷 | 障害に気づけない | アプリ側は fire-and-forget で監視失敗に影響されない設計 (§6.6)。monotir が死んだことは Play Console の Android vitals と Cloud Logging で代替検知できるようにしておく。 |
| ブログ記事の画像が Cloud Storage 依存 | アカウント削除で記事のリンクが切れる | 設定画面とアカウント削除の確認画面で明記。将来はてなフォトライフへのアップロードを検討 (§14 M-4)。 |

---

## 14. 決定事項と未決事項

### 14.1 決定事項 (2026-09-12)

| # | 論点 | 決定 | 反映先 |
|---|------|------|--------|
| D-1 | 収益化 | **行わない。** 課金・広告・アフィリエイトなしの非商用ツール。規模は個人〜数人。 | §1.3, §4, §10, §13 |
| D-2 | 監視基盤 | **自作の monotir を使う。** 外部 APM (Crashlytics / Sentry 等) は導入しない。 | §5.2, §6.6, §11 |
| D-3 | 食べログ連携 | **URL の保存とリンク表示のみ。** ページ取得・スクレイピング・メタデータ抽出は行わない。店舗補完は Places API に一本化。 | §6.3, §13 |
| D-4 | ブログ連携先 | **はてなブログのみ。** WordPress / note / Micropub は対象外。 | §6.5 |
| D-5 | スプレッドシート | **出力の一方向のみ。** シート側の編集をアプリへ取り込む双方向同期はしない。正本は Firestore。 | §6.4 |

### 14.2 未決事項 (要確認)

| # | 論点 | 補足 |
|---|------|------|
| M-1 | **monotir の連携方式** | §6.6 は「HTTP + JSON のイベント受信エンドポイント」を仮定して書いている。実際のプロトコル (OTLP / 独自 JSON / ログ転送)、認証方式、メトリクスのモデル (イベント指向 / メトリクス指向)、アラート設定の方法が分かり次第 §6.6 と §11 を書き換える。**M0 の完了条件**。 |
| M-2 | iOS 対応 | 将来対応するか。するなら Android 側を Kotlin Multiplatform を見据えた層分けにしておくか。 |
| M-3 | 共有・公開機能 (F-406) | 他人に見せる需要があるか。あるなら公開 URL の粒度 (記録単位 / 月単位 / 全体)。はてなブログ投稿があれば不要かもしれない。 |
| M-4 | ブログ記事の画像ホスティング | Cloud Storage 直リンク (実装が軽い / 削除でリンク切れ) か、はてなフォトライフへのアップロード (実装が重い / 記事が自己完結) か。§6.5 は前者を既定としている。 |
| M-5 | ランチ以外の食事 | 夕食・カフェも記録したくなる可能性。`records.mealType` を最初から持たせて後方互換を確保するか。 |
| M-6 | 写真の原本保持 | 端末ストレージとクラウド費用のトレードオフ。既定を「リサイズのみ保存」とするか「原本も保持」とするか。非商用で自腹なので前者を推奨。 |
| M-7 | ソース公開 | OSS として公開するか。する場合、API キーや monotir エンドポイントの設定を外出しする必要がある (§6.6 の「エンドポイント未設定時は無効化」はこれを見越した仕様)。 |
