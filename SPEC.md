# ランチログ (Lunch Log) 仕様書

毎日のランチ (と、その他の食事) を写真とともに記録し、Web で振り返り、
スプレッドシートやはてなブログへ書き出せる、作者自身のための Android アプリ。

- 文書バージョン: 0.4 (ドラフト)
- 最終更新: 2026-09-12
- ステータス: 仕様検討中 / 未実装

---

## 1. 背景と目的

### 1.1 解決したい課題

- 毎日ランチを食べているが、「先週どこで何を食べたか」を思い出せない。
- 写真はカメラロールに溜まるが、店名・メニュー名と紐づかず検索できない。
- 記録アプリは入力項目が多くて続かない。**入力の手間が最大の敵**。
- 食べ歩きの記録を自分の資産 (スプレッドシート / ブログ) として残したいが、
  既存サービスに閉じ込められたくない。

### 1.2 プロダクトの狙い

1. **3 タップで記録が終わる**こと。撮る → 候補から店を選ぶ → 保存。
2. 位置情報・EXIF・メニュー写真の OCR から、**人間が打つ文字を極小化**する。
3. 記録したデータは**いつでも外に出せる** (Sheets / ブログ / JSON エクスポート)。

### 1.3 非目標 (スコープ外)

- グルメ SNS・レビュー投稿プラットフォーム (他人のレビューを読む機能)。
- 店舗予約、クーポン、決済。
- カロリー・栄養素の自動算出 (将来検討)。
- **収益化** — 課金・広告・アフィリエイトは一切行わない。作者自身のための
  非商用ツールとして設計する。
- **iOS 対応** — Android 専用とする。Kotlin Multiplatform も考慮しない
  (層分けのために設計を歪めない)。
- **Web での不特定多数への公開** — Web は**自分専用の閲覧・編集画面**であり、
  ログイン必須。限定公開 URL や共有ページは作らない。外部への発信は
  はてなブログ投稿 (§6.5) が担う。
- 食べログのコンテンツ (点数・レビュー・店舗詳細) の取り込み。URL の保存のみ行う (§6.3)。

> **ランチ以外の食事について:** 朝食・夕食・カフェ・間食も記録できる
> (§3.1 F-116)。ただし UI とサジェストの既定値はランチに最適化し、
> 「平日の昼を最速で記録する」体験を損なわないことを優先する。

---

## 2. ターゲットとユースケース

### 2.1 想定ユーザー

- **主たる利用者は作者自身。** 平日ランチを外食・テイクアウトし、その記録を
  自分の資産として残したい人。
- Android + Google アカウント利用者 (Android 専用。iOS 対応はしない)。
- Web は同一アカウントでログインする**自分専用**の画面。他人に見せる用途は持たない。

### 2.2 主要ユースケース

| ID | ユースケース | 頻度 |
|----|--------------|------|
| UC-1 | 着席後にメニュー表を撮影 → メニュー名の候補を得る | 毎回 |
| UC-2 | 料理が来たら撮影 → 位置情報から店が自動で埋まる → 保存 | 毎日 |
| UC-3 | 後から (帰社後・帰宅後) 写真を選んでまとめて記録する | 週数回 |
| UC-4 | 食べログの URL を共有シートからアプリに送り、店舗へのリンクとして残す | 時々 |
| UC-5 | Web でカレンダー / 一覧を眺め、先月の記録を振り返る | 週 1 |
| UC-6 | 月末にスプレッドシートへ同期して経費・支出を集計する | 月 1 |
| UC-7 | 気に入った店をはてなブログの記事として下書き投稿する | 月数回 |
| UC-8 | 出張先の夕食や休日のカフェも、食事種別を変えて同じアプリに記録する | 時々 |
| UC-9 | Web で写真の原本を開き、拡大して細部を確認する / 別用途にダウンロードする | 時々 |

### 2.3 代表的な体験フロー (UC-2)

```
[ホーム] --(FABタップ)--> [カメラ]
   撮影 (料理写真)
        |
        v
[記録編集画面]  ← 位置情報から近隣店舗を取得し「店名」を候補表示
   店名: ◯◯食堂 (自動、ワンタップで確定)
   メニュー: (メニュー写真OCR/過去履歴から候補)
   金額: (任意)
        |
    [保存] ← ここまで平均 15 秒 / タップ 3 回以内
        v
[ホーム] 本日のカードが追加される
```

---

## 3. 機能要件

優先度: **P0** = MVP 必須 / **P1** = 初期リリース時に欲しい / **P2** = 将来。

### 3.1 記録 (Android)

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-101 | 料理写真の撮影・添付 | P0 | CameraX による撮影、またはギャラリーから選択。1 記録あたり最大 10 枚。 |
| F-102 | メニュー写真の撮影・添付 | P0 | 写真に `種別 = メニュー表` を付与。料理写真と区別して保存・表示。 |
| F-103 | メニュー名の入力 | P0 | 自由入力 + 候補サジェスト (自分の過去入力 / 同店の他記録 / OCR 結果)。 |
| F-104 | 店名の入力 | P0 | 自由入力 + 位置情報からの候補サジェスト。 |
| F-105 | 位置情報の取得と店舗補完 | P0 | 端末 GPS または写真 EXIF の GPS から近隣飲食店を検索し候補提示。 |
| F-106 | 日時の記録 | P0 | 既定は写真の撮影日時 (EXIF)、無ければ保存時刻。手動変更可。 |
| F-107 | 金額の記録 | P1 | 数値入力。通貨は JPY 固定 (将来多通貨)。 |
| F-108 | 評価・メモ・タグ | P1 | 5 段階評価、自由メモ、タグ (#ラーメン #新規開拓 など)。 |
| F-109 | 食べログ URL の保存 | P1 | URL を貼付 / 共有インテント受信で店舗に紐づけて保存。取得・解析はしない (§6.3)。 |
| F-110 | メニュー写真の OCR | P1 | ML Kit Text Recognition (日本語) でメニュー候補を抽出しチップ表示。 |
| F-111 | 下書き / オフライン保存 | P0 | 圏外でもローカルに保存し、オンライン復帰時に自動同期。 |
| F-112 | 記録の編集・削除 | P0 | 削除は論理削除 (30 日間はゴミ箱から復元可)。 |
| F-113 | 共有インテント受信 | P1 | 他アプリ (カメラ・ブラウザ) からの画像 / URL 共有でアプリを起動。 |
| F-114 | ホーム画面ウィジェット / クイックタイル | P2 | ワンタップで撮影記録を開始。 |
| F-115 | リマインダー通知 | P2 | 平日 13:00 に「今日のランチ記録しませんか」(ユーザー設定で ON/OFF)。 |
| F-116 | 食事種別の記録 | P0 | `ランチ / 朝食 / 夕食 / カフェ / 間食 / その他` から選択。既定は記録時刻から推定 (11〜15 時ならランチ)。1 タップで変更可。 |
| F-117 | 写真原本の保持 | P1 | リサイズ版とは別に撮影原本を保管し、Web から閲覧・ダウンロードできる (§6.7)。 |

### 3.2 閲覧・検索 (Android / Web 共通)

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-201 | タイムライン表示 | P0 | 日付降順のカード一覧。無限スクロール。 |
| F-202 | 写真グリッド表示 | P1 | 料理写真だけを格子状に表示。 |
| F-203 | カレンダー表示 | P1 | 月カレンダー上に各日のサムネイル。 |
| F-204 | 記録詳細 | P0 | 写真スライド、店名、メニュー、金額、評価、メモ、食事種別、地図。Web では原本表示へ切替可。 |
| F-205 | 検索 | P1 | 店名 / メニュー名 / メモ / タグの部分一致検索。 |
| F-206 | フィルタ | P1 | 期間、評価、タグ、店舗、金額レンジ、**食事種別**。既定はランチのみ表示 (切替可)。 |
| F-207 | 店舗別まとめ | P1 | 「この店で食べたもの」一覧と訪問回数。 |
| F-208 | 統計ダッシュボード | P2 | 月間支出、訪問店舗数、ジャンル比率、リピート率。食事種別ごとに集計。 |
| F-209 | 地図表示 | P2 | 記録をピンで地図に配置。 |

### 3.3 アカウント・同期

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-301 | Google ログイン | P0 | Firebase Authentication (Google プロバイダ) で Android / Web 共通ログイン。 |
| F-302 | 複数端末同期 | P0 | 同一アカウントの Android と Web で同じデータを参照。 |
| F-303 | ログアウト / アカウント削除 | P0 | 削除時は全データと画像を 30 日以内に完全消去。 |
| F-304 | 匿名利用 → 後からログイン | P2 | 匿名アカウントで開始し、ログイン時にデータを引き継ぐ。 |

### 3.4 エクスポート・連携

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-401 | Google スプレッドシート出力 | P1 | 記録を 1 行 1 レコードで追記。初回にシートを自動生成。**出力の一方向のみ** (§6.4)。 |
| F-402 | スプレッドシート自動出力 | P2 | 新規記録を日次バッチで自動追記。 |
| F-403 | はてなブログ投稿 | P1 | はてなブログへ AtomPub で下書き投稿 (§6.5)。他ブログサービスは対象外。 |
| F-404 | Markdown / HTML 生成 | P1 | 日次・月次まとめを Markdown で生成しコピー / 共有。 |
| F-405 | CSV / JSON エクスポート | P1 | 全データのダウンロード (写真は URL または ZIP)。 |

---

## 4. 非機能要件

| 分類 | 要件 |
|------|------|
| 性能 | アプリ起動からカメラ表示まで 2 秒以内。保存操作のローカル完了は 300ms 以内 (アップロードは非同期)。 |
| オフライン | 記録の作成・閲覧・編集はオフラインで可能。同期は WorkManager による再試行付きキュー。 |
| 通信量 | 表示用に長辺 2048px / JPEG 品質 85 のリサイズ版とサムネイルを生成して先に送る。**原本は別途アップロードする** (既定は Wi-Fi 接続時のみ、設定で常時に変更可)。 |
| ストレージ | 原本を保持するため 1 記録あたり約 3〜6MB を見込む。月 20 記録 × 数人で年間 5GB 程度。Cloud Storage の無料枠 (5GB) を超えたら課金される前提でコスト監視する (§13)。 |
| 対応 OS | Android 10 (API 29) 以上。ターゲット SDK は最新安定版。 |
| Web | 自分専用のため対象は Chrome (PC) を主とし、Safari / Edge の最新 2 バージョンも動作させる。モバイルブラウザでも崩れない程度のレスポンシブ対応に留める。 |
| 可用性 | マネージドサービス依存。SLA はプラットフォーム準拠。個人利用規模を想定。 |
| セキュリティ | 全通信 HTTPS。Firestore / Storage はセキュリティルールで所有者のみアクセス可。 |
| プライバシー | 位置情報は任意。EXIF 位置情報の除去オプション。既定は完全非公開。 |
| 国際化 | 文言は日本語を第一言語とし、リソース分離して英語追加可能な構造にする。 |
| アクセシビリティ | コントラスト比 4.5:1 以上、TalkBack 対応のコンテンツ説明。 |
| コスト | 非商用ツールとして、数人規模 / 1 人あたり月 20 記録で各サービスの無料枠に収まることを目標とする。超過しやすいのは Places API と Cloud Storage (§13)。 |
| 監視 | **M6 まで実装しない。** それまでは Play Console の Android vitals と Cloud Logging の標準機能で足りるものとする。M6 で自作基盤 **`monitor`** に集約する (§6.6)。外部 APM は使わない。 |

---

## 5. システム構成

### 5.1 全体像

```
  ┌────────────────┐        ┌────────────────┐
  │ Android アプリ  │        │  Web (Next.js) │
  │ Kotlin/Compose │        │   React/SSR    │
  └───────┬────────┘        └───────┬────────┘
          │  Firebase SDK           │
          ├─────────────┬───────────┤
          v             v           v
  ┌────────────┐ ┌────────────┐ ┌──────────────┐
  │  Firebase  │ │ Firestore  │ │   Firebase   │
  │    Auth    │ │    (DB)    │ │   Storage    │
  └────────────┘ └─────┬──────┘ └──────────────┘
                       │ トリガ
                       v
              ┌──────────────────┐    ┌──────────────────┐
              │ Cloud Functions  │───>│ Google Sheets    │ (出力のみ)
              │  / Cloud Run     │───>│ はてなブログ      │ (AtomPub/下書き)
              │                  │───>│ Places API       │ (店舗補完)
              └────────┬─────────┘    └──────────────────┘
                       │
                       │ (M6) イベント / エラー / 所要時間
       Android ────────┼────────> ┌────────────────────────┐
       Web ────────────┘          │ monitor (自作監視/後回し) │
                                  └────────────────────────┘
```

### 5.2 技術選定

| レイヤ | 採用 | 理由 |
|--------|------|------|
| Android | Kotlin / Jetpack Compose / MVVM + UseCase | 最新の標準構成。宣言的 UI で開発速度が出る。 |
| ローカル DB | Room | オフラインファーストの Single Source of Truth。 |
| 同期 | WorkManager + Firestore オフライン永続化 | プロセス終了後も再試行される。 |
| カメラ | CameraX | 端末差異の吸収。 |
| 画像読み込み | Coil | Compose 親和性。 |
| OCR | ML Kit Text Recognition v2 (日本語) | オンデバイスで無料・オフライン可。 |
| 認証 | Firebase Authentication (Google) | Android / Web 共通、Credential Manager 対応。 |
| DB | Cloud Firestore | リアルタイム同期とオフラインキャッシュが標準装備。 |
| ファイル | Cloud Storage for Firebase | 画像保管。セキュリティルールで保護。 |
| サーバ処理 | Cloud Functions (TypeScript) | 外部 API 呼び出しと秘密鍵の隠蔽。 |
| Web | Next.js (App Router) + Firebase Hosting | 自分専用・ログイン必須のため SEO と OGP は不要。SSR は使わずクライアントレンダリング中心で構成を単純に保つ。 |
| 地図・店舗 | Google Places API (New) / Maps SDK | 位置情報からの店舗補完。 |
| 監視 | Android vitals / Cloud Logging (M1〜M5) → **`monitor`** (M6) | M5 までは標準機能で済ませ、監視基盤の導入は後回しにする (§6.6)。外部 APM は導入しない。 |

> **代替案:** Firestore ではなく Cloud Run + PostgreSQL(PostGIS) にすると、
> 複雑な集計・地理検索・全文検索が容易になる。将来、統計機能 (F-208) が
> 主要価値になる場合は移行を検討する。MVP は開発速度を優先し Firebase を採用。

---

## 6. 外部連携仕様

### 6.1 位置情報による店舗補完 (F-105)

**入力**
1. 端末の現在地 (FusedLocationProvider、精度優先 / 10 秒タイムアウト)。
2. 写真 EXIF の GPS タグ (後から登録する場合に使用)。

**処理**
1. 取得した緯度経度で Places API `searchNearby` を呼ぶ (Cloud Functions 経由)。
   - `includedPrimaryTypes: ["restaurant", "cafe", "bakery", "meal_takeaway"]`
   - 半径 150m (取得できなければ 300m まで自動拡大)
   - `rankPreference: DISTANCE`、最大 10 件
2. 以下でスコアリングし並べ替える。
   - 距離 (近いほど高)
   - ユーザーの過去訪問履歴 (訪問済みは大きく加点)
   - 記録時刻が営業時間内か
3. 上位 5 件を候補チップとして表示。1 タップで店名・住所・placeId・座標を確定。

**フォールバック**
- 位置情報権限が無い / 取得失敗 → 直近に訪れた店の履歴を候補として表示。
- どれにも当たらない → 自由入力 (新規店舗として登録)。

**プライバシー**
- 位置情報権限は「記録時のみ (WHEN_IN_USE)」。バックグラウンド追跡はしない。
- 権限拒否でもアプリの全機能が手動入力で利用できること。

### 6.2 メニュー写真 OCR (F-110)

1. メニュー写真を ML Kit でオンデバイス OCR。
2. テキストブロックを行単位に整形し、次のルールでメニュー候補を抽出。
   - 価格らしき数値 (`¥?[0-9,]{3,5}円?`) を含む行 → その行の非数値部分をメニュー名候補、数値を価格候補とする。
   - 1〜30 文字程度の行を候補に含める。
3. 候補をチップで提示。タップでメニュー名 / 金額に反映。
4. OCR 全文は `photos.ocrText` に保存し、検索対象に含める (P2)。

### 6.3 食べログ URL の保存 (F-109)

**方針: URL を保存するだけ。食べログへのアクセス・解析は一切行わない。**

食べログは第三者向けの公開 API を提供しておらず、ページの取得・解析は
利用規約や robots.txt に抵触しうる。本サービスはスクレイピングも
メタデータ取得も行わず、**ユーザーが貼り付けた URL をそのまま保管する**
だけに留める。これにより法務リスクを完全に回避し、外部サイトの構造変更に
よる故障も起きない。

**仕様**

1. 記録編集画面と店舗編集画面に「食べログ URL」欄を置く。
2. 入力方法は 2 つ。
   - 手動でペースト。
   - ブラウザの共有メニューから URL をアプリへ送る (`ACTION_SEND` / `text/plain`)。
     `tabelog.com` ドメインの URL を受け取ったら、新規記録の下書きを開き
     URL 欄を埋めた状態で表示する。
3. 保存先は `restaurants.tabelogUrl` (店舗に 1 つ)。記録側からは店舗経由で参照する。
4. バリデーションはクライアント内で完結させる。
   - スキームが `https`、ホストが `tabelog.com` またはそのサブドメインであること。
   - クエリパラメータ (トラッキング ID 等) は除去して正規化する。
   - 条件を満たさない URL は「食べログの URL ではないようです」と警告するが、
     保存自体は許可する (ユーザーの判断を尊重)。
5. 表示は**リンクのみ**。記録詳細・店舗詳細に「食べログで見る ↗」ボタンを置き、
   タップで外部ブラウザ / カスタムタブを開く。ページ内容の埋め込みやプレビューはしない。

**店名・住所の補完はどこから来るか**

食べログからは取らないため、店舗情報の自動補完は以下に一本化する。

- 位置情報からの Places API `searchNearby` (§6.1) — 主経路。
- 店名テキストからの Places API `searchText` — ユーザーが店名を打った場合の補完。
- 上記で見つからなければ手入力。

つまり「食べログ URL は店舗への外部リンクとして保存されるだけで、
入力補助の情報源ではない」。URL を貼っても店名欄は自動で埋まらない。

### 6.4 Google スプレッドシート出力 (F-401)

> **方向は一方向 (アプリ → シート) のみ。** シート側での編集をアプリへ取り込む
> 双方向同期は行わない。シートは「持ち出し先・集計用のビュー」であり、
> 正本は常に Firestore のデータとする。

**認可**
- Firebase の Google ログインとは別に、**増分認可 (incremental auth)** で
  `https://www.googleapis.com/auth/drive.file` スコープを追加取得する。
  - `drive.file` はアプリが作成したファイルのみアクセス可能で、ユーザーの
    既存 Drive 全体を読まないため、プライバシー上最も望ましい。
- リフレッシュトークンは Cloud Functions 側で暗号化して保管 (Secret Manager / KMS)。

**シート構成**
- 初回連携時に `ランチログ` という名前のスプレッドシートを新規作成。
- 出力対象は既定で**ランチのみ**。設定で全食事種別に変更できる。
- シート名は `records`。1 行目はヘッダ固定。

| 列 | 内容 | 例 |
|----|------|-----|
| A | 記録ID | `rec_01HX...` |
| B | 日付 | `2026-09-12` |
| C | 時刻 | `12:35` |
| D | 食事種別 | `ランチ` |
| E | 店名 | `◯◯食堂` |
| F | メニュー名 | `日替わり定食` |
| G | 金額 | `980` |
| H | 評価 | `4` |
| I | タグ | `定食,リピート` |
| J | メモ | `味噌汁がうまい` |
| K | 住所 | `東京都渋谷区…` |
| L | 緯度 | `35.6581` |
| M | 経度 | `139.7016` |
| N | 食べログURL | `https://tabelog.com/…` |
| O | 料理写真URL | 表示用 (リサイズ版) の URL |
| P | メニュー写真URL | 同上 |
| Q | 最終更新 | ISO8601 |

- 写真の原本 URL は列に含めない。原本は Web から辿るものとし、シートには
  表示用 URL だけを置く (列を増やしすぎないため)。

**出力方式**
- 手動出力: 「スプレッドシートに書き出す」ボタンで未出力分を書き込む。
- 差分管理: A 列の記録 ID をキーに、既存行があれば `values.update` で上書き、
  無ければ `values.append` で追記する (upsert)。
- 自動出力 (F-402): 日次バッチ (Cloud Scheduler → Functions) で前日分をまとめて反映。
- ユーザーがシートを手で編集していた場合、アプリが書き込む A〜Q 列は
  次回出力時に上書きされる。**R 列以降はアプリが触らない**ため、
  ユーザーの追加列 (自作の集計式やメモ) は保持される。この仕様を設定画面に明記する。
- シートの削除・破損時は「シートを作り直す」操作で再生成し、全件を再出力する。

### 6.5 はてなブログ連携 (F-403)

**対象ははてなブログのみ。** WordPress / note / Micropub などは対象外とする
(将来必要になれば §6.5 を拡張する)。連携先を持たないユーザー向けには
Markdown 書き出し (F-404) をフォールバックとして提供する。

**認証**

- 設定画面で **はてな ID** と **API キー** (はてなの「アカウント設定 → AtomPub」で発行)
  を登録する。ブログ ID (`example.hatenablog.com`) も入力する。
- AtomPub は WSSE 認証または Basic 認証に対応する。**Basic (HTTPS 上)** を採用し、
  実装を単純に保つ。
- 認証情報は Cloud Functions が Secret Manager に保存し、**クライアントへは返さない**。
  設定画面には「登録済み / 未登録」の状態だけを表示する。
- 登録時に `GET /{hatena-id}/{blog-id}/atom/entry` を 1 回叩いて疎通確認を行い、
  失敗したら保存しない。

**投稿**

- エンドポイント: `POST https://blog.hatena.ne.jp/{hatena-id}/{blog-id}/atom/entry`
- ボディは Atom XML:

```xml
<?xml version="1.0" encoding="utf-8"?>
<entry xmlns="http://www.w3.org/2005/Atom"
       xmlns:app="http://www.w3.org/2007/app">
  <title>2026-09-12 のランチ</title>
  <author><name>{hatena-id}</name></author>
  <content type="text/x-markdown">{{本文}}</content>
  <updated>2026-09-12T12:35:00+09:00</updated>
  <category term="ランチ" />
  <category term="◯◯食堂" />
  <app:control>
    <app:draft>yes</app:draft>
  </app:control>
</entry>
```

- **常に下書き (`<app:draft>yes</app:draft>`) で投稿する。** 公開はユーザーが
  はてなブログ側で行う。誤爆を防ぐためこの既定は変更できない設定とする
  (将来「公開で投稿」を足す場合は明示的なオプトインにする)。
- 投稿成功時、レスポンスの `<link rel="edit">` と `<link rel="alternate">` を
  `records.sync.blog` に保存し、記録詳細から編集画面へ飛べるようにする。
- 再投稿は `PUT {edit-url}` で同じ記事を更新する (記事の重複を作らない)。

**写真の扱い — はてなフォトライフへ同時アップロード**

記事本文から Cloud Storage を直接参照すると、アプリ側のデータを消したときに
記事の画像が消える。これを避けるため、**投稿時に画像をはてなフォトライフへ
アップロードし、記事はフォトライフ上の画像を参照する**。これで記事は
はてな側で自己完結する。

手順:

1. 記事に含める写真 (既定は料理写真の 1 枚目。編集画面で追加・変更可) を
   Cloud Storage から取得する。
   - アップロードするのは**表示用のリサイズ版** (長辺 2048px)。原本は
     フォトライフには送らない (容量と転送量の節約。原本は Web で見られる)。
2. はてなフォトライフ AtomPub API に `POST` する。
   - エンドポイント: `https://f.hatena.ne.jp/atom/post`
   - 認証は **WSSE**。ブログ側 (§ 上記「認証」) も WSSE に揃えると、
     認証ヘッダ生成を 1 箇所に共通化できる。
   - ボディは画像を Base64 で埋めた Atom XML:

```xml
<entry xmlns="http://purl.org/atom/ns#">
  <title>2026-09-12 ◯◯食堂 日替わり定食</title>
  <content mode="base64" type="image/jpeg">{{base64}}</content>
</entry>
```

3. レスポンスの Atom entry に含まれる画像 URL (`hatena:imageurl` 相当) と
   エントリ URL を取り出し、`photos[].hatenaFotolife` に保存する。
4. 記事本文の `{{dishPhotoUrl}}` をフォトライフの URL に差し替えて投稿する。

**要確認 (実装時):** フォトライフ AtomPub の正確な要素名・認証方式・
1 日あたりのアップロード上限は、はてなの公式ドキュメントで確認すること。
本節は一般に知られている仕様に基づく記述であり、裏取りをしていない。

**失敗時の扱い**

- フォトライフへのアップロードに失敗したら、**記事投稿を中止**し
  「画像のアップロードに失敗しました」と表示する。画像なしの記事を
  勝手に投稿しない。
- 同じ写真を 2 回投稿しないよう、アップロード済みの `photos[].hatenaFotolife`
  が存在すれば再利用する (記事を更新する `PUT` でも重複アップしない)。

**記事テンプレート (ユーザー編集可能)**

```markdown
# {{date}} のランチ

## {{restaurantName}} / {{dishName}}

![{{dishName}}]({{dishPhotoUrl}})

- 店名: {{restaurantName}}{{#tabelogUrl}} ([食べログ]({{tabelogUrl}})){{/tabelogUrl}}
- メニュー: {{dishName}}
- 金額: {{price}}円
- 評価: {{stars}}

{{memo}}
```

- 投稿単位は「1 記録 = 1 記事」「1 日 = 1 記事」「1 ヶ月 = 1 記事」から選択可能。
- テンプレートは Mustache 風の記法とし、設定画面で編集できる。
  未設定の項目 (金額・評価など) の行は自動的に省かれる。

### 6.6 監視連携 (`monitor`) — **M6 に後回し**

監視は自作の監視基盤 **`monitor`** に集約する。ただし**着手は M6 とし、
M1〜M5 では監視のコードを一切書かない**。仕様の検討自体も M6 まで行わない。

理由: 自分ひとりが毎日使うツールであり、不具合は使っていれば気づく。
監視を先に作っても、記録が貯まっていない段階では見るものがない。

**M1〜M5 で監視の代わりに使うもの** (いずれも追加実装が不要)

| 見たいもの | 手段 |
|------------|------|
| アプリのクラッシュ | Google Play Console の Android vitals |
| Functions のエラー・実行ログ | Cloud Logging (既定で記録される) |
| 日次バッチの失敗 | Cloud Scheduler の実行履歴 / Cloud Logging のエラーログ |
| 費用の異常 | GCP の予算アラート (§13) |

**M6 で決めること** (§14.2 M-1)

- プロトコル (OTLP / 独自 JSON / ログ転送など)、エンドポイント、認証方式。
- データモデル (イベント指向かメトリクス指向か)、送信頻度とバッファリング。
- アラートの設定方法と通知先。
- 送る項目 — 候補は §11.2 に控えてある。

**M6 まで持ち越す原則** (方式が変わっても揺らがない前提として、先に決めておく)

- 外部 APM (Crashlytics / Sentry など) は導入しない。
- 個人を特定できる情報は送らない (`uid`・店名・メニュー名・写真・位置情報)。
- 監視の失敗がアプリの失敗にならない (fire-and-forget、未設定なら無効化)。

> M1〜M5 のコードに監視用のフックを埋め込んでおく必要はない。
> M6 の着手時に、そのとき本当に見たい項目だけを後から差し込む。
> 先回りして `MonitoringSink` のような抽象を用意することもしない
> (使われないインタフェースが残るほうが害になる)。

### 6.7 写真の保存と原本の保持 (F-117)

写真は **3 つの解像度**を Cloud Storage に保存する。

| 種類 | サイズ | 生成場所 | 用途 |
|------|--------|----------|------|
| サムネイル | 長辺 320px / JPEG 70 | 端末 | 一覧・グリッド表示 |
| 表示用 | 長辺 2048px / JPEG 85 | 端末 | 詳細表示、ブログ投稿、シートの写真 URL |
| **原本** | 撮影されたまま | — | Web での拡大表示・ダウンロード、将来の再加工 |

**アップロードの順序と条件**

1. 保存操作の直後、サムネイルと表示用を即座にアップロードする (小さいので速い)。
2. 原本は WorkManager のキューに積み、**既定では Wi-Fi 接続時かつ充電中**に
   アップロードする。設定で「常にアップロード」「原本を保存しない」に変更できる。
3. 原本のアップロードが完了するまで、端末内の元ファイルは削除しない。
   完了後に `photos[].originalUploadedAt` を立てる。

**Web での閲覧 (UC-9)**

- 記録詳細では既定で表示用を出す。「原本を表示」で原本に切り替える。
- 原本が未アップロードなら「この写真の原本はまだ端末にあります」と表示する。
- ダウンロードボタンから原本を保存できる (Cloud Storage の署名付き URL、有効期限 1 時間)。

**EXIF の扱い**

- 表示用とサムネイルからは、既定で **GPS 情報のみ**を除去する
  (撮影日時と向きは残す。日時は F-106 で使うため)。
- **原本は一切加工せず、EXIF を含めてそのまま保存する。** 原本は自分しか
  見られない領域 (`users/{uid}/originals/`) に置き、ブログやシートには
  原本 URL を出さないため、GPS が外部へ漏れる経路がない。
- 設定「原本も GPS を除去する」を用意する (既定 OFF)。

**容量対策**

- 原本保持により 1 記録あたり 3〜6MB を見込む。設定画面に現在の使用容量を表示する。
- 古い原本の自動削除は行わない (勝手に消さない)。容量が問題になったら
  「◯年以前の原本を削除」という明示的な操作を提供する (P2)。

---

## 7. データモデル

Firestore のコレクション構成 (`{}` はドキュメント ID)。

```
users/{uid}
users/{uid}/records/{recordId}
users/{uid}/restaurants/{restaurantId}   # ユーザーごとの店舗キャッシュ
users/{uid}/integrations/{provider}      # sheets / hatena / wordpress
restaurantsPublic/{placeId}              # 共有マスタ (P2)
```

### 7.1 `users/{uid}`

```jsonc
{
  "displayName": "onishi",
  "email": "user@example.com",
  "photoUrl": "https://…",
  "locale": "ja-JP",
  "timeZone": "Asia/Tokyo",
  "settings": {
    "defaultCurrency": "JPY",
    "stripExifOnUpload": true,        // 表示用/サムネの GPS 除去
    "stripExifOnOriginal": false,     // 原本からも除去するか (既定 OFF)
    "keepOriginals": true,            // 原本を保持するか (§6.7)
    "originalUploadPolicy": "wifi_charging", // wifi_charging | wifi | always
    "defaultMealType": "auto",        // auto (時刻から推定) または固定値
    "listMealTypeFilter": ["lunch"],  // 一覧の既定フィルタ
    "reminderEnabled": false,
    "reminderTime": "13:00",
    "autoExportSheets": false,
    "sheetsExportMealTypes": ["lunch"]
  },
  "createdAt": "2026-09-12T02:00:00Z",
  "updatedAt": "2026-09-12T02:00:00Z"
}
```

### 7.2 `records/{recordId}`

```jsonc
{
  "id": "rec_01HX...",              // ULID
  "eatenAt": "2026-09-12T03:35:00Z", // UTC。表示はユーザー TZ
  "mealType": "lunch",               // lunch | breakfast | dinner | cafe | snack | other
  "dishName": "日替わり定食",
  "restaurantName": "◯◯食堂",        // 非正規化 (表示高速化のため)
  "restaurantId": "rst_01HX...",     // null 可
  "price": 980,
  "currency": "JPY",
  "rating": 4,                        // 1-5、null 可
  "memo": "味噌汁がうまい",
  "tags": ["定食", "リピート"],
  "photos": [
    {
      "id": "pho_01HX...",
      "kind": "dish",                // dish | menu | exterior | receipt
      "thumbPath":    "users/{uid}/thumbs/pho_01HX.jpg",
      "displayPath":  "users/{uid}/photos/pho_01HX.jpg",     // 長辺2048
      "originalPath": "users/{uid}/originals/pho_01HX.jpg",  // 原本。未アップロードなら null
      "originalBytes": 4823104,
      "originalUploadedAt": "2026-09-12T12:10:00Z",
      "width": 4032, "height": 3024,   // 原本の寸法
      "takenAt": "2026-09-12T03:34:00Z",
      "ocrText": null,
      "hatenaFotolife": {              // §6.5 でアップロード済みの場合
        "imageUrl": "https://cdn-ak.f.st-hatena.com/images/fotolife/…",
        "entryUrl": "https://f.hatena.ne.jp/…"
      },
      "order": 0
    }
  ],
  "location": {
    "lat": 35.6581, "lng": 139.7016,
    "geohash": "xn76urx",
    "accuracy": 12.5,
    "source": "device"               // device | exif | place | manual
  },
  "source": "camera",                // camera | gallery | share | web
  "visibility": "private",           // private | unlisted
  "sync": {
    "sheets": { "syncedAt": null, "rowId": null },
    "blog":   { "postedAt": null, "url": null, "editUrl": null, "provider": "hatena" }
  },
  "createdAt": "…", "updatedAt": "…", "deletedAt": null
}
```

### 7.3 `restaurants/{restaurantId}`

```jsonc
{
  "id": "rst_01HX...",
  "name": "◯◯食堂",
  "nameKana": "マルマルショクドウ",
  "googlePlaceId": "ChIJ...",
  "tabelogUrl": "https://tabelog.com/tokyo/A1303/…/",
  "address": "東京都渋谷区…",
  "lat": 35.6581, "lng": 139.7016,
  "genres": ["定食", "和食"],
  "nearestStation": "渋谷",
  "visitCount": 7,
  "lastVisitedAt": "2026-09-12T03:35:00Z",
  "createdAt": "…", "updatedAt": "…"
}
```

### 7.4 `integrations/{provider}`

```jsonc
{
  "provider": "sheets",
  "enabled": true,
  "config": { "spreadsheetId": "1AbC…", "sheetName": "records" },
  "secretRef": "projects/…/secrets/sheets-token-{uid}/versions/latest",
  "lastSyncedAt": "2026-09-12T04:00:00Z",
  "lastError": null
}
```

### 7.5 インデックス

- `records`: `mealType ASC, eatenAt DESC` (既定の一覧。ランチのみ表示)
- `records`: `eatenAt DESC` (全種別の一覧)
- `records`: `restaurantId ASC, eatenAt DESC` (店舗別)
- `records`: `tags ARRAY, eatenAt DESC` (タグ絞り込み)
- `records`: `deletedAt ASC, eatenAt DESC` (ゴミ箱除外)
- 全文検索は MVP ではクライアント側の部分一致で対応。データ量増加時に
  Algolia または Firestore 拡張機能の導入を検討。

### 7.6 セキュリティルール (方針)

```
match /users/{uid}/{document=**} {
  allow read, write: if request.auth != null && request.auth.uid == uid;
}
```

- Storage も同様に `users/{uid}/**` を所有者のみに限定。
- 写真原本のダウンロード (§6.7) は Cloud Functions が発行する有効期限 1 時間の
  署名付き URL 経由のみ。恒久的に公開される URL は発行しない。

---

## 8. API 仕様 (Cloud Functions)

クライアントから直接叩けない処理のみサーバに置く。認証は Firebase ID トークン (Bearer)。

| メソッド | パス | 概要 |
|----------|------|------|
| `POST` | `/v1/places/nearby` | 緯度経度から近隣飲食店候補を返す (Places API ラッパ)。 |
| `POST` | `/v1/places/search` | テキストから店舗を検索。 |
| `POST` | `/v1/integrations/sheets/connect` | OAuth コールバック処理とシート初期化。 |
| `POST` | `/v1/integrations/sheets/export` | 未出力レコードをシートへ upsert (一方向)。 |
| `POST` | `/v1/integrations/hatena/connect` | はてな ID / API キーの疎通確認と保存。 |
| `POST` | `/v1/integrations/hatena/publish` | 画像をフォトライフへ上げ、記事を生成してブログへ下書き投稿。 |
| `GET`  | `/v1/export?format=json\|csv` | 全記録のエクスポート。 |
| `POST` | `/v1/account/delete` | アカウントとデータの削除ジョブ登録。 |

**共通エラー形式**

```jsonc
{ "error": { "code": "PLACES_QUOTA_EXCEEDED",
             "message": "店舗候補を取得できませんでした",
             "retryable": true } }
```

- レート制限: 1 ユーザーあたり 60 req/分。超過時 `429`。
- 外部 API 障害時はクライアントを手動入力にフォールバックさせる (機能を止めない)。
- M5 までエラーは Cloud Logging に出すだけとし、`monitor` への送信は M6 で追加する (§6.6)。

---

## 9. 画面仕様

### 9.1 Android

| 画面 | 内容 |
|------|------|
| スプラッシュ / ログイン | Google ログインボタンのみ。初回のみ表示。 |
| ホーム (タイムライン) | 日付ごとのセクション、記録カード (写真・店名・メニュー・金額)。右下に FAB (カメラ)。下部ナビ: ホーム / 検索 / 統計 / 設定。 |
| カメラ | CameraX プレビュー。シャッター、ギャラリー選択、種別切替 (料理 / メニュー)。 |
| 記録編集 | 写真サムネイル列、店名 (候補チップ)、メニュー名 (候補チップ)、金額、評価、タグ、メモ、日時、**食事種別** (既定は時刻から推定)、食べログ URL 欄 (リンク保存のみ)。上部に「保存」。 |
| 記録詳細 | 写真ページャ、各項目、地図サムネイル、編集 / 削除 / 共有 / ブログ投稿。原本の同期状態を小さく表示。 |
| 検索 | 検索窓 + フィルタチップ + 結果リスト。 |
| 店舗詳細 | 店舗情報、訪問回数、その店の記録一覧。 |
| 統計 | 月間支出、記録数、ジャンル比率、よく行く店 Top5。 |
| 設定 | アカウント、連携 (Sheets / はてなブログ)、通知、写真 (原本アップロード条件・使用容量)、プライバシー (EXIF 除去)、エクスポート、ログアウト、アカウント削除。 |

**入力最小化の設計原則**
- 保存に必須なのは「写真 1 枚」または「メニュー名」のどちらか一方のみ。
- 店名・日時・位置は自動補完され、ユーザーは確認するだけ。
- 候補チップは常に「最も選ばれそうなもの」を左端に置く。

### 9.2 Web

| 画面 | 内容 |
|------|------|
| ログイン | Google ログイン。 |
| 一覧 | タイムライン / グリッド / カレンダーの 3 表示切替。食事種別で絞り込み。 |
| 詳細 | 写真拡大、**原本表示への切替とダウンロード** (§6.7)、全項目表示、編集。 |
| 検索・フィルタ | 期間・タグ・店舗・評価・食事種別。 |
| 統計 | 月次グラフ。 |
| 設定 | 連携設定、エクスポート、ストレージ使用量。 |

- Web からの新規作成も可能 (写真アップロード + 手入力)。位置補完は
  ブラウザ Geolocation または写真 EXIF を使用。
- **Web は自分専用。** 全ページがログイン必須で、未ログイン時はログイン画面だけを出す。
  公開ページ・共有 URL・OGP 対応は持たない (§1.3)。そのぶん SSR や OGP の
  作り込みが不要になり、実装が軽くなる。

---

## 10. 権限・プライバシー・法務

| 項目 | 方針 |
|------|------|
| Android 権限 | `CAMERA` (必須)、`ACCESS_FINE_LOCATION` (任意)、`POST_NOTIFICATIONS` (任意)。ストレージは Photo Picker を使い権限不要にする。 |
| 権限説明 | 初回リクエスト前に「なぜ必要か」を画面で説明してから OS ダイアログを出す。 |
| 位置情報 | 記録時のみ取得。保存しない設定も提供。既定の公開範囲は非公開。 |
| EXIF | 表示用・サムネイルは GPS のみ除去 (既定 ON)。**原本は無加工で保存**し、自分専用領域に置く (§6.7)。原本からも除去する設定を別途用意 (既定 OFF)。 |
| データ所有 | ユーザーはいつでも全データをエクスポート・削除できる。写真は原本を含めてダウンロードできる。 |
| 削除 | アカウント削除要求から 30 日以内に Firestore / Storage (原本を含む) / 連携トークンを完全消去。はてなフォトライフへアップロード済みの画像は、はてな側に残る旨を削除確認画面で明示する (アプリからは消せない)。 |
| 第三者提供 | しない。監視データは自前の `monitor` にのみ送り、個人特定情報は含めない (§6.6)。外部 APM / 広告 SDK は組み込まない。 |
| 外部 API 規約 | Google Maps Platform の利用規約 (キャッシュ制限・帰属表示) を遵守。食べログは URL の保存とリンク表示のみで、ページ取得・解析は行わない (§6.3)。 |
| 収益 | 非商用。課金・広告・アフィリエイトリンクを置かない。食べログ URL もアフィリエイトタグを付けない (正規化時にクエリを除去する)。 |
| 店舗写真 | メニュー表の撮影は店舗により禁止されている場合がある旨をアプリ内で注意喚起。 |
| 必要書類 | プライバシーポリシー、利用規約、Play Console のデータセーフティ申告。 |

---

## 11. 品質の見方

### 11.1 M1〜M5 — 標準機能だけで見る

監視基盤は M6 まで作らない (§6.6)。それまでの品質確認は次で行う。

- **自分が毎日使う。** 記録が面倒になった、遅くなった、落ちた、は使えば分かる。
  これが最も確実な検知手段であり、ひとり用ツールではこれで足りる。
- **Play Console の Android vitals** — クラッシュ率と ANR。追加実装不要。
- **Cloud Logging** — Functions のエラーと実行ログ。既定で記録される。
  シート出力・ブログ投稿の失敗はここに出す。
- **GCP の予算アラート** — Places API と Cloud Storage の費用 (§13)。

### 11.2 M6 以降 — `monitor` へ送る項目の候補

**M6 で改めて取捨選択する**ための控え。今この通りに実装する必要はない。

| イベント名 | 種別 | 用途 |
|------------|------|------|
| `time_to_save_ms` | duration | カメラ起動から保存完了まで。**最重要指標** |
| `record_created` | counter | 記録数。属性: source / has_photo / has_location / autofilled_restaurant |
| `restaurant_suggestion_accepted` / `_rejected` | counter | 店舗候補の精度 |
| `places_nearby_ms` | duration | Places API 応答時間 |
| `sheets_export_completed` / `_failed` | counter | シート出力の成否 |
| `hatena_publish_completed` / `_failed` | counter | ブログ投稿の成否 |
| `fotolife_upload_ms` / `_failed` | duration / counter | フォトライフへの画像アップロード |
| `original_upload_queue_depth` | counter | 未アップロードの原本枚数 (§6.7) |
| `sync_queue_depth` | counter | 未同期レコード数 (オフライン同期の健全性) |
| `unhandled_error` | error | 未捕捉例外 |

アラート候補: 日次バッチが 2 日連続で失敗 / `unhandled_error` の急増 /
シート出力・ブログ投稿の連続失敗。

### 11.3 目標値 (自分が使い続けられているかの確認。事業指標ではない)

- 週あたりの記録数 — 平日の 8 割以上。
- 記録 1 件あたりの所要時間 — 中央値 15 秒以内。
- 店舗名の自動補完採用率 — 70% 以上。
- 同期失敗率 — 1% 未満。

> M5 までは計測手段がないため、これらは**体感で判断する**。
> 数値で追えるようになるのは M6 以降。

### 11.4 テスト

- ユニット: ViewModel / UseCase / OCR 解析ロジック / スプレッドシート行変換 / 食べログ URL の検証と正規化 / Atom XML 生成 / WSSE ヘッダ生成 / 食事種別の時刻推定。
- 結合: Firestore エミュレータ + Functions エミュレータ。
- UI: Compose UI Test で「撮影 → 保存」のハッピーパス。
- 手動: 圏外 → 復帰時の同期、権限拒否時の動作、大量データ (1,000 件) のスクロール、Wi-Fi 切断下での原本アップロード保留と復帰。

---

## 12. 開発マイルストーン

| フェーズ | 内容 | 完了条件 |
|----------|------|----------|
| M0: 設計 | 本仕様の確定、Firebase プロジェクト作成、CI 整備 | 仕様レビュー完了 |
| M1: MVP | F-101〜F-106, F-109, F-111, F-112, F-116, F-201, F-204, F-301, F-302 | 自分で毎日使える |
| M2: 補完強化 | F-105 高度化, F-110, F-107, F-108, F-205, F-206 | 入力が 15 秒以内 |
| M3: Web + 写真原本 | Web 閲覧一式 (F-201〜F-207)、F-117 と §6.7 | PC から振り返れる / 原本を取り出せる |
| M4: 連携 | F-401, F-403, F-404, F-405 | シート / ブログへ出せる |
| M5: 拡張 | F-208, F-209, F-402, F-114 ウィジェット, F-115 通知 | — |
| M6: 監視 | `monitor` の連携方式を決めて実装 (§6.6, §11.2)。**M5 までは着手しない** | 異常に自分で気づく前に通知が来る |

---

## 13. リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| 食べログから店舗情報を自動取得できない | 店名・住所の手入力が残る | URL 保存のみと割り切った上で、Places API による補完 (§6.1) の精度向上に投資する。`restaurant_suggestion_accepted` を監視し、採用率が 70% を下回るならスコアリングを見直す。 |
| Places API のコスト増 | 運用費 (非商用なので自腹) | 呼び出しをサーバ集約し、同一座標 (100m グリッド) の結果を 24 時間キャッシュ。セッショントークンを活用。GCP に予算アラートを設定し、上限超過時は候補提示を無効化して手入力に落とす。 |
| 入力が面倒で継続されない | 離脱 | 保存必須項目を 1 つに絞る。`time_to_save_ms` を継続監視。 |
| OCR 精度が低い | 期待外れ | あくまで「候補提示」に留め、手入力を常に一級市民として扱う。 |
| Firestore の集計・検索が弱い | 機能制約 | 統計は事前集計ドキュメントを Functions で更新。必要なら PostgreSQL へ移行。 |
| はてな API キーの漏洩 | 重大 (ブログを勝手に投稿される) | Secret Manager 管理、クライアントへ返さない、失効時は再認証を促す。投稿は常に下書きに限定し、被害範囲を下書き汚染に留める。 |
| M5 まで監視がない | 不具合・失敗に気づくのが遅れる | 自分が毎日使うため体感で気づけるものと割り切る。加えて Android vitals・Cloud Logging・予算アラートを M1 から有効にしておく (§11.1)。シート出力とブログ投稿の失敗は**アプリ内に明示表示**し、ログを見なくても分かるようにする。 |
| `monitor` 自体が落ちる / 自作ゆえの運用負荷 | 障害に気づけない (M6 以降) | アプリ側は fire-and-forget で監視失敗に影響されない設計 (§6.6)。`monitor` が死んでも Android vitals と Cloud Logging で代替検知できる状態を保つ。 |
| フォトライフ API の仕様が想定と違う / 上限に当たる | ブログ投稿が動かない | 実装前に公式ドキュメントで裏取りする (§6.5 の「要確認」)。アップロード失敗時は記事投稿ごと中止し、画像なし記事を作らない。 |
| 写真原本によるストレージ肥大 | Cloud Storage の無料枠 (5GB) 超過で課金 | 設定画面に使用容量を常時表示。原本アップロードは既定で Wi-Fi + 充電中に限定。「原本を保存しない」設定も用意。GCP の予算アラートを設定する。 |
| 食事種別の追加で「ランチアプリ」の焦点がぼやける | 入力が遅くなる | 既定値を時刻から推定し (11〜15 時はランチ)、種別変更は 1 タップに留める。一覧の既定フィルタはランチのみ。`time_to_save_ms` が悪化しないか監視する。 |

---

## 14. 決定事項と未決事項

### 14.1 決定事項

| # | 論点 | 決定 | 反映先 |
|---|------|------|--------|
| D-1 | 収益化 | **行わない。** 課金・広告・アフィリエイトなしの非商用ツール。規模は個人〜数人。 | §1.3, §4, §10, §13 |
| D-2 | 監視基盤 | **自作の `monitor` を使う。ただし着手は M6 に後回し。** M5 までは Android vitals と Cloud Logging の標準機能で済ませ、監視コードを書かない。外部 APM は導入しない。 | §5.2, §6.6, §11, §12 |
| D-3 | 食べログ連携 | **URL の保存とリンク表示のみ。** ページ取得・スクレイピング・メタデータ抽出は行わない。店舗補完は Places API に一本化。 | §6.3, §13 |
| D-4 | ブログ連携先 | **はてなブログのみ。** WordPress / note / Micropub は対象外。 | §6.5 |
| D-5 | スプレッドシート | **出力の一方向のみ。** シート側の編集をアプリへ取り込む双方向同期はしない。正本は Firestore。 | §6.4 |
| D-6 | iOS 対応 | **しない。** Android 専用。Kotlin Multiplatform も考慮せず、そのために設計を歪めない。 | §1.3, §2.1 |
| D-7 | Web の位置づけ | **自分専用の閲覧・編集画面。** 全ページログイン必須。公開ページ・共有 URL・OGP 対応は作らない (F-406 を削除)。SSR も不要。 | §1.3, §5.2, §9.2 |
| D-8 | ランチ以外の食事 | **記録できるようにする。** `mealType` (ランチ / 朝食 / 夕食 / カフェ / 間食 / その他) を P0 で持つ。既定値は時刻から推定し、一覧の既定フィルタはランチ。 | §3.1 F-116, §7.2 |
| D-9 | 写真の原本 | **保持する。** サムネ / 表示用 / 原本の 3 段構成で、原本は Web から閲覧・ダウンロードできる。原本アップロードは既定で Wi-Fi + 充電中。 | §6.7, §3.1 F-117 |
| D-10 | はてなフォトライフ | **同時にアップロードする** (レビュー承認済み)。ブログ記事はフォトライフ上の画像を参照し、はてな側で自己完結させる。表示用のリサイズ版のみ送り、原本は送らない。アップロード失敗時は記事投稿ごと中止する。 | §6.5 |


### 14.2 未決事項 (要確認)

| # | 論点 | 補足 |
|---|------|------|
| M-1 | **`monitor` の連携方式** | プロトコル、エンドポイント、認証方式、データモデル、アラート設定。**M6 の着手時に検討する。それまで議論しない** (§6.6)。M1〜M5 のコードに監視用のフックを先回りで入れることもしない。 |
| M-2 | はてなフォトライフ API の実仕様 | §6.5 はエンドポイント `https://f.hatena.ne.jp/atom/post` / WSSE 認証 / Base64 埋め込みという一般に知られた仕様で書いているが、裏取りしていない。実装前に公式ドキュメントで、要素名・認証方式・1 日あたりのアップロード上限・容量制限を確認する。**M4 の着手条件**。 |
| M-3 | 原本を保存しない選択肢の既定値 | 現状は「原本を保存する (既定 ON)」「Wi-Fi + 充電中のみアップロード」。容量が現実にどれだけ増えるかを M3 で実測し、既定を見直す。 |
| M-4 | 食事種別の粒度 | `ランチ / 朝食 / 夕食 / カフェ / 間食 / その他` の 6 種で足りるか。「飲み会」「弁当 (持参)」「テイクアウト」などを分けたくなるか。タグ (F-108) で代用する手もある。 |
| M-5 | カロリー・栄養素 | 現状は非目標。将来やるならメニュー名からの推定か手入力か。 |
| M-6 | ソース公開 | OSS として公開するか。する場合、API キーと `monitor` エンドポイントの設定を外出しする必要がある (§6.6 の「未設定なら無効化」はこれを見越した仕様)。 |
