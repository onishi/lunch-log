# ランチログ (Lunch Log) 仕様書

毎日のランチを写真とともに記録し、Web で振り返り、スプレッドシートやブログへ書き出せるサービス。

- 文書バージョン: 0.1 (ドラフト)
- 最終更新: 2026-09-12
- ステータス: 仕様検討中 / 未実装

---

## 1. 背景と目的

### 1.1 解決したい課題

- 毎日ランチを食べているが、「先週どこで何を食べたか」を思い出せない。
- 写真はカメラロールに溜まるが、店名・メニュー名と紐づかず検索できない。
- 記録アプリは入力項目が多くて続かない。**入力の手間が最大の敵**。
- 食べ歩きの記録を自分の資産 (スプレッドシート / ブログ) として残したいが、
  既存サービスに閉じ込められたくない。

### 1.2 プロダクトの狙い

1. **3 タップで記録が終わる**こと。撮る → 候補から店を選ぶ → 保存。
2. 位置情報・EXIF・OCR・食べログ URL から、**人間が打つ文字を極小化**する。
3. 記録したデータは**いつでも外に出せる** (Sheets / ブログ / JSON エクスポート)。

### 1.3 非目標 (スコープ外)

- グルメ SNS・レビュー投稿プラットフォーム (他人のレビューを読む機能)。
- 店舗予約、クーポン、決済。
- ランチ以外の食事の網羅的な記録 (夕食・朝食も保存自体は可能だが最適化しない)。
- カロリー・栄養素の自動算出 (将来検討)。

---

## 2. ターゲットとユースケース

### 2.1 想定ユーザー

- オフィスワーカー / リモートワーカーで、平日ランチを外食・テイクアウトする人。
- 自分のライフログを残したい、ブログやスプレッドシートを使い慣れた層。
- Android + Google アカウント利用者 (初期は Android のみ)。

### 2.2 主要ユースケース

| ID | ユースケース | 頻度 |
|----|--------------|------|
| UC-1 | 着席後にメニュー表を撮影 → メニュー名の候補を得る | 毎回 |
| UC-2 | 料理が来たら撮影 → 位置情報から店が自動で埋まる → 保存 | 毎日 |
| UC-3 | 後から (帰社後・帰宅後) 写真を選んでまとめて記録する | 週数回 |
| UC-4 | 食べログの URL を共有シートからアプリに送り、店情報を取り込む | 時々 |
| UC-5 | Web でカレンダー / 一覧を眺め、先月の記録を振り返る | 週 1 |
| UC-6 | 月末にスプレッドシートへ同期して経費・支出を集計する | 月 1 |
| UC-7 | 気に入った店をブログ記事として下書き投稿する | 月数回 |

### 2.3 代表的な体験フロー (UC-2)

```
[ホーム] --(FABタップ)--> [カメラ]
   撮影 (料理写真)
        |
        v
[記録編集画面]  ← 位置情報から近隣店舗を取得し「店名」を候補表示
   店名: ◯◯食堂 (自動、ワンタップで確定)
   メニュー: (メニュー写真OCR/過去履歴から候補)
   金額: (任意)
        |
    [保存] ← ここまで平均 15 秒 / タップ 3 回以内
        v
[ホーム] 本日のカードが追加される
```

---

## 3. 機能要件

優先度: **P0** = MVP 必須 / **P1** = 初期リリース時に欲しい / **P2** = 将来。

### 3.1 記録 (Android)

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-101 | 料理写真の撮影・添付 | P0 | CameraX による撮影、またはギャラリーから選択。1 記録あたり最大 10 枚。 |
| F-102 | メニュー写真の撮影・添付 | P0 | 写真に `種別 = メニュー表` を付与。料理写真と区別して保存・表示。 |
| F-103 | メニュー名の入力 | P0 | 自由入力 + 候補サジェスト (自分の過去入力 / 同店の他記録 / OCR 結果)。 |
| F-104 | 店名の入力 | P0 | 自由入力 + 位置情報からの候補サジェスト。 |
| F-105 | 位置情報の取得と店舗補完 | P0 | 端末 GPS または写真 EXIF の GPS から近隣飲食店を検索し候補提示。 |
| F-106 | 日時の記録 | P0 | 既定は写真の撮影日時 (EXIF)、無ければ保存時刻。手動変更可。 |
| F-107 | 金額の記録 | P1 | 数値入力。通貨は JPY 固定 (将来多通貨)。 |
| F-108 | 評価・メモ・タグ | P1 | 5 段階評価、自由メモ、タグ (#ラーメン #新規開拓 など)。 |
| F-109 | 食べログ URL からの入力補助 | P1 | URL を貼付 / 共有インテント受信で店名・住所・ジャンルを補完 (§6.3)。 |
| F-110 | メニュー写真の OCR | P1 | ML Kit Text Recognition (日本語) でメニュー候補を抽出しチップ表示。 |
| F-111 | 下書き / オフライン保存 | P0 | 圏外でもローカルに保存し、オンライン復帰時に自動同期。 |
| F-112 | 記録の編集・削除 | P0 | 削除は論理削除 (30 日間はゴミ箱から復元可)。 |
| F-113 | 共有インテント受信 | P1 | 他アプリ (カメラ・ブラウザ) からの画像 / URL 共有でアプリを起動。 |
| F-114 | ホーム画面ウィジェット / クイックタイル | P2 | ワンタップで撮影記録を開始。 |
| F-115 | リマインダー通知 | P2 | 平日 13:00 に「今日のランチ記録しませんか」(ユーザー設定で ON/OFF)。 |

### 3.2 閲覧・検索 (Android / Web 共通)

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-201 | タイムライン表示 | P0 | 日付降順のカード一覧。無限スクロール。 |
| F-202 | 写真グリッド表示 | P1 | 料理写真だけを格子状に表示。 |
| F-203 | カレンダー表示 | P1 | 月カレンダー上に各日のサムネイル。 |
| F-204 | 記録詳細 | P0 | 写真スライド、店名、メニュー、金額、評価、メモ、地図。 |
| F-205 | 検索 | P1 | 店名 / メニュー名 / メモ / タグの部分一致検索。 |
| F-206 | フィルタ | P1 | 期間、評価、タグ、店舗、金額レンジ。 |
| F-207 | 店舗別まとめ | P1 | 「この店で食べたもの」一覧と訪問回数。 |
| F-208 | 統計ダッシュボード | P2 | 月間支出、訪問店舗数、ジャンル比率、リピート率。 |
| F-209 | 地図表示 | P2 | 記録をピンで地図に配置。 |

### 3.3 アカウント・同期

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-301 | Google ログイン | P0 | Firebase Authentication (Google プロバイダ) で Android / Web 共通ログイン。 |
| F-302 | 複数端末同期 | P0 | 同一アカウントの Android と Web で同じデータを参照。 |
| F-303 | ログアウト / アカウント削除 | P0 | 削除時は全データと画像を 30 日以内に完全消去。 |
| F-304 | 匿名利用 → 後からログイン | P2 | 匿名アカウントで開始し、ログイン時にデータを引き継ぐ。 |

### 3.4 エクスポート・連携

| ID | 機能 | 優先度 | 内容 |
|----|------|--------|------|
| F-401 | Google スプレッドシート同期 | P1 | 記録を 1 行 1 レコードで追記。初回にシートを自動生成 (§6.4)。 |
| F-402 | スプレッドシート自動同期 | P2 | 新規記録を即時 or 日次バッチで自動追記。 |
| F-403 | ブログ投稿 | P1 | はてなブログ / WordPress へ記事を下書き投稿 (§6.5)。 |
| F-404 | Markdown / HTML 生成 | P1 | 日次・月次まとめを Markdown で生成しコピー / 共有。 |
| F-405 | CSV / JSON エクスポート | P1 | 全データのダウンロード (写真は URL または ZIP)。 |
| F-406 | 公開ページ | P2 | 記録を限定公開 URL で共有 (個別 / 月次)。 |

---

## 4. 非機能要件

| 分類 | 要件 |
|------|------|
| 性能 | アプリ起動からカメラ表示まで 2 秒以内。保存操作のローカル完了は 300ms 以内 (アップロードは非同期)。 |
| オフライン | 記録の作成・閲覧・編集はオフラインで可能。同期は WorkManager による再試行付きキュー。 |
| 通信量 | 写真は端末側で長辺 2048px / JPEG 品質 85 にリサイズしてからアップロード (原本保持はオプション)。 |
| 対応 OS | Android 10 (API 29) 以上。ターゲット SDK は最新安定版。 |
| Web | Chrome / Safari / Edge の最新 2 バージョン。モバイルブラウザ対応 (レスポンシブ)。 |
| 可用性 | マネージドサービス依存。SLA はプラットフォーム準拠。個人利用規模を想定。 |
| セキュリティ | 全通信 HTTPS。Firestore / Storage はセキュリティルールで所有者のみアクセス可。 |
| プライバシー | 位置情報は任意。EXIF 位置情報の除去オプション。既定は完全非公開。 |
| 国際化 | 文言は日本語を第一言語とし、リソース分離して英語追加可能な構造にする。 |
| アクセシビリティ | コントラスト比 4.5:1 以上、TalkBack 対応のコンテンツ説明。 |
| コスト | 想定 1,000 ユーザー / 月 20 記録で月額数千円以内に収まる構成を選ぶ。 |

---

## 5. システム構成

### 5.1 全体像

```
  ┌────────────────┐        ┌────────────────┐
  │ Android アプリ  │        │  Web (Next.js) │
  │ Kotlin/Compose │        │   React/SSR    │
  └───────┬────────┘        └───────┬────────┘
          │  Firebase SDK           │
          ├─────────────┬───────────┤
          v             v           v
  ┌────────────┐ ┌────────────┐ ┌──────────────┐
  │  Firebase  │ │ Firestore  │ │   Firebase   │
  │    Auth    │ │    (DB)    │ │   Storage    │
  └────────────┘ └─────┬──────┘ └──────────────┘
                       │ トリガ
                       v
              ┌──────────────────┐    ┌─────────────────┐
              │ Cloud Functions  │───>│ Google Sheets   │
              │  / Cloud Run     │───>│ ブログ AtomPub  │
              │                  │───>│ Places API      │
              │                  │───>│ 食べログ URL解析 │
              └──────────────────┘    └─────────────────┘
```

### 5.2 技術選定

| レイヤ | 採用 | 理由 |
|--------|------|------|
| Android | Kotlin / Jetpack Compose / MVVM + UseCase | 最新の標準構成。宣言的 UI で開発速度が出る。 |
| ローカル DB | Room | オフラインファーストの Single Source of Truth。 |
| 同期 | WorkManager + Firestore オフライン永続化 | プロセス終了後も再試行される。 |
| カメラ | CameraX | 端末差異の吸収。 |
| 画像読み込み | Coil | Compose 親和性。 |
| OCR | ML Kit Text Recognition v2 (日本語) | オンデバイスで無料・オフライン可。 |
| 認証 | Firebase Authentication (Google) | Android / Web 共通、Credential Manager 対応。 |
| DB | Cloud Firestore | リアルタイム同期とオフラインキャッシュが標準装備。 |
| ファイル | Cloud Storage for Firebase | 画像保管。セキュリティルールで保護。 |
| サーバ処理 | Cloud Functions (TypeScript) | 外部 API 呼び出しと秘密鍵の隠蔽。 |
| Web | Next.js (App Router) + Firebase Hosting | SSR/ISR による高速表示と OGP 対応。 |
| 地図・店舗 | Google Places API (New) / Maps SDK | 位置情報からの店舗補完。 |
| 監視 | Firebase Crashlytics / Performance / Cloud Logging | 障害検知。 |

> **代替案:** Firestore ではなく Cloud Run + PostgreSQL(PostGIS) にすると、
> 複雑な集計・地理検索・全文検索が容易になる。将来、統計機能 (F-208) が
> 主要価値になる場合は移行を検討する。MVP は開発速度を優先し Firebase を採用。

---

## 6. 外部連携仕様

### 6.1 位置情報による店舗補完 (F-105)

**入力**
1. 端末の現在地 (FusedLocationProvider、精度優先 / 10 秒タイムアウト)。
2. 写真 EXIF の GPS タグ (後から登録する場合に使用)。

**処理**
1. 取得した緯度経度で Places API `searchNearby` を呼ぶ (Cloud Functions 経由)。
   - `includedPrimaryTypes: ["restaurant", "cafe", "bakery", "meal_takeaway"]`
   - 半径 150m (取得できなければ 300m まで自動拡大)
   - `rankPreference: DISTANCE`、最大 10 件
2. 以下でスコアリングし並べ替える。
   - 距離 (近いほど高)
   - ユーザーの過去訪問履歴 (訪問済みは大きく加点)
   - 記録時刻が営業時間内か
3. 上位 5 件を候補チップとして表示。1 タップで店名・住所・placeId・座標を確定。

**フォールバック**
- 位置情報権限が無い / 取得失敗 → 直近に訪れた店の履歴を候補として表示。
- どれにも当たらない → 自由入力 (新規店舗として登録)。

**プライバシー**
- 位置情報権限は「記録時のみ (WHEN_IN_USE)」。バックグラウンド追跡はしない。
- 権限拒否でもアプリの全機能が手動入力で利用できること。

### 6.2 メニュー写真 OCR (F-110)

1. メニュー写真を ML Kit でオンデバイス OCR。
2. テキストブロックを行単位に整形し、次のルールでメニュー候補を抽出。
   - 価格らしき数値 (`¥?[0-9,]{3,5}円?`) を含む行 → その行の非数値部分をメニュー名候補、数値を価格候補とする。
   - 1〜30 文字程度の行を候補に含める。
3. 候補をチップで提示。タップでメニュー名 / 金額に反映。
4. OCR 全文は `photos.ocrText` に保存し、検索対象に含める (P2)。

### 6.3 食べログ URL からの入力補助 (F-109)

> **重要な前提:** 食べログは第三者向けの公開 API を提供していない。
> スクレイピングは利用規約・robots.txt に抵触する可能性があるため、
> **実装前に必ず最新の規約と robots.txt を確認すること**。本節は
> 「規約上許容される範囲での実装」を前提に、段階的な方針を定める。

**方針 A (既定・推奨): URL の保存とメタデータの最小取得**
- ユーザーが貼付/共有した URL をそのまま `restaurant.tabelogUrl` に保存する。
- Cloud Functions がその URL を 1 回だけ取得し、**ページが自ら公開している構造化データのみ**を読む。
  - OGP (`og:title` / `og:description` / `og:image`)
  - JSON-LD (`schema.org/Restaurant` の `name` / `address` / `geo` / `telephone`)
- 取得結果は「候補」としてユーザーに提示し、**確定は必ずユーザー操作**で行う (誤取得の責任範囲を明確化)。
- レスポンスは 7 日間キャッシュし、同一 URL への再アクセスを避ける。
- User-Agent を明示し、robots.txt を尊重。1 ユーザーあたり 10 req/分のレート制限。
- 取得失敗時は URL のみ保存して手動入力にフォールバック (機能停止しない)。

**方針 B (補完): Google Places による名寄せ**
- URL 内の店舗名テキストを Places API `searchText` に渡し、住所・座標・ジャンルを補完する。
- 食べログ側から取得する情報を最小化できるため、方針 A より安全。

**方針 C (将来): 公式提携 / アフィリエイト API**
- 提携プログラム経由で API 提供がある場合はそちらへ切り替える。

**UI**
- 記録編集画面に「食べログ URL を貼り付け」フィールド。
- ブラウザの共有メニューからアプリへ URL を送ると、新規記録の下書きが作られる。
- 取得できた項目はハイライトし、「この内容で埋める」ボタンで反映。

### 6.4 Google スプレッドシート連携 (F-401)

**認可**
- Firebase の Google ログインとは別に、**増分認可 (incremental auth)** で
  `https://www.googleapis.com/auth/drive.file` スコープを追加取得する。
  - `drive.file` はアプリが作成したファイルのみアクセス可能で、ユーザーの
    既存 Drive 全体を読まないため、プライバシー上最も望ましい。
- リフレッシュトークンは Cloud Functions 側で暗号化して保管 (Secret Manager / KMS)。

**シート構成**
- 初回連携時に `ランチログ` という名前のスプレッドシートを新規作成。
- シート名は `records`。1 行目はヘッダ固定。

| 列 | 内容 | 例 |
|----|------|-----|
| A | 記録ID | `rec_01HX...` |
| B | 日付 | `2026-09-12` |
| C | 時刻 | `12:35` |
| D | 店名 | `◯◯食堂` |
| E | メニュー名 | `日替わり定食` |
| F | 金額 | `980` |
| G | 評価 | `4` |
| H | タグ | `定食,リピート` |
| I | メモ | `味噌汁がうまい` |
| J | 住所 | `東京都渋谷区…` |
| K | 緯度 | `35.6581` |
| L | 経度 | `139.7016` |
| M | 食べログURL | `https://tabelog.com/…` |
| N | 料理写真URL | (署名付き or 公開 URL) |
| O | メニュー写真URL | |
| P | 最終更新 | ISO8601 |

**同期方式**
- 手動同期: 「スプレッドシートに書き出す」ボタンで未同期分を `values.append`。
- 差分管理: 記録 ID をキーに、既存行があれば更新、無ければ追記 (upsert)。
- 自動同期 (F-402): Firestore の onWrite トリガから 5 分間隔のバッチでまとめて反映。
- 競合: ユーザーがシートを手で編集していても、アプリ側は自分が書いた列のみ上書きする。

### 6.5 ブログ連携 (F-403)

対応先 (優先順):

1. **はてなブログ** — AtomPub API。`はてなID` + `APIキー` を設定画面で登録。
   - `POST /{hatena-id}/{blog-id}/atom/entry` に Atom XML を送信。
   - 既定は下書き (`<app:draft>yes</app:draft>`)。
2. **WordPress** — REST API v2。サイト URL + ユーザー名 + アプリケーションパスワード。
   - `POST /wp-json/wp/v2/posts` (`status: draft`)。
   - 画像は `POST /wp-json/wp/v2/media` で先にアップロードし、本文から参照。
3. **Markdown 書き出し** (F-404) — 連携先を持たないユーザー向けのフォールバック。

**記事テンプレート (ユーザー編集可能)**

```
# {{date}} のランチ

## {{restaurantName}} / {{dishName}}

![{{dishName}}]({{dishPhotoUrl}})

- 店名: [{{restaurantName}}]({{tabelogUrl}})
- メニュー: {{dishName}}
- 金額: {{price}}円
- 評価: {{stars}}

{{memo}}
```

- 投稿単位は「1 記録 = 1 記事」または「1 日 = 1 記事」「1 ヶ月 = 1 記事」を選択可能。
- 認証情報は Secret Manager に保管し、クライアントには一切返さない。
- 投稿は常に**下書き**を既定とし、公開はユーザーがブログ側で行う (誤爆防止)。

---

## 7. データモデル

Firestore のコレクション構成 (`{}` はドキュメント ID)。

```
users/{uid}
users/{uid}/records/{recordId}
users/{uid}/restaurants/{restaurantId}   # ユーザーごとの店舗キャッシュ
users/{uid}/integrations/{provider}      # sheets / hatena / wordpress
restaurantsPublic/{placeId}              # 共有マスタ (P2)
```

### 7.1 `users/{uid}`

```jsonc
{
  "displayName": "onishi",
  "email": "user@example.com",
  "photoUrl": "https://…",
  "locale": "ja-JP",
  "timeZone": "Asia/Tokyo",
  "settings": {
    "defaultCurrency": "JPY",
    "stripExifOnUpload": true,
    "reminderEnabled": false,
    "reminderTime": "13:00",
    "autoSyncSheets": false
  },
  "createdAt": "2026-09-12T02:00:00Z",
  "updatedAt": "2026-09-12T02:00:00Z"
}
```

### 7.2 `records/{recordId}`

```jsonc
{
  "id": "rec_01HX...",              // ULID
  "eatenAt": "2026-09-12T03:35:00Z", // UTC。表示はユーザー TZ
  "dishName": "日替わり定食",
  "restaurantName": "◯◯食堂",        // 非正規化 (表示高速化のため)
  "restaurantId": "rst_01HX...",     // null 可
  "price": 980,
  "currency": "JPY",
  "rating": 4,                        // 1-5、null 可
  "memo": "味噌汁がうまい",
  "tags": ["定食", "リピート"],
  "photos": [
    {
      "id": "pho_01HX...",
      "kind": "dish",                // dish | menu | exterior | receipt
      "storagePath": "users/{uid}/photos/pho_01HX.jpg",
      "thumbPath":   "users/{uid}/thumbs/pho_01HX.jpg",
      "width": 2048, "height": 1536,
      "takenAt": "2026-09-12T03:34:00Z",
      "ocrText": null,
      "order": 0
    }
  ],
  "location": {
    "lat": 35.6581, "lng": 139.7016,
    "geohash": "xn76urx",
    "accuracy": 12.5,
    "source": "device"               // device | exif | place | manual
  },
  "source": "camera",                // camera | gallery | share | web
  "visibility": "private",           // private | unlisted
  "sync": {
    "sheets": { "syncedAt": null, "rowId": null },
    "blog":   { "postedAt": null, "url": null, "provider": null }
  },
  "createdAt": "…", "updatedAt": "…", "deletedAt": null
}
```

### 7.3 `restaurants/{restaurantId}`

```jsonc
{
  "id": "rst_01HX...",
  "name": "◯◯食堂",
  "nameKana": "マルマルショクドウ",
  "googlePlaceId": "ChIJ...",
  "tabelogUrl": "https://tabelog.com/tokyo/A1303/…/",
  "address": "東京都渋谷区…",
  "lat": 35.6581, "lng": 139.7016,
  "genres": ["定食", "和食"],
  "nearestStation": "渋谷",
  "visitCount": 7,
  "lastVisitedAt": "2026-09-12T03:35:00Z",
  "createdAt": "…", "updatedAt": "…"
}
```

### 7.4 `integrations/{provider}`

```jsonc
{
  "provider": "sheets",
  "enabled": true,
  "config": { "spreadsheetId": "1AbC…", "sheetName": "records" },
  "secretRef": "projects/…/secrets/sheets-token-{uid}/versions/latest",
  "lastSyncedAt": "2026-09-12T04:00:00Z",
  "lastError": null
}
```

### 7.5 インデックス

- `records`: `eatenAt DESC` (一覧)
- `records`: `restaurantId ASC, eatenAt DESC` (店舗別)
- `records`: `tags ARRAY, eatenAt DESC` (タグ絞り込み)
- `records`: `deletedAt ASC, eatenAt DESC` (ゴミ箱除外)
- 全文検索は MVP ではクライアント側の部分一致で対応。データ量増加時に
  Algolia または Firestore 拡張機能の導入を検討。

### 7.6 セキュリティルール (方針)

```
match /users/{uid}/{document=**} {
  allow read, write: if request.auth != null && request.auth.uid == uid;
}
```

- Storage も同様に `users/{uid}/**` を所有者のみに限定。
- 限定公開 (F-406) は Cloud Functions が発行する署名付き URL 経由のみ。

---

## 8. API 仕様 (Cloud Functions)

クライアントから直接叩けない処理のみサーバに置く。認証は Firebase ID トークン (Bearer)。

| メソッド | パス | 概要 |
|----------|------|------|
| `POST` | `/v1/places/nearby` | 緯度経度から近隣飲食店候補を返す (Places API ラッパ)。 |
| `POST` | `/v1/places/search` | テキストから店舗を検索。 |
| `POST` | `/v1/tabelog/resolve` | 食べログ URL から公開メタデータを抽出 (§6.3)。 |
| `POST` | `/v1/integrations/sheets/connect` | OAuth コールバック処理とシート初期化。 |
| `POST` | `/v1/integrations/sheets/sync` | 未同期レコードをシートへ upsert。 |
| `POST` | `/v1/integrations/blog/publish` | 記事を生成しブログへ下書き投稿。 |
| `GET`  | `/v1/export?format=json\|csv` | 全記録のエクスポート。 |
| `POST` | `/v1/account/delete` | アカウントとデータの削除ジョブ登録。 |

**共通エラー形式**

```jsonc
{ "error": { "code": "PLACES_QUOTA_EXCEEDED",
             "message": "店舗候補を取得できませんでした",
             "retryable": true } }
```

- レート制限: 1 ユーザーあたり 60 req/分。超過時 `429`。
- 外部 API 障害時はクライアントを手動入力にフォールバックさせる (機能を止めない)。

---

## 9. 画面仕様

### 9.1 Android

| 画面 | 内容 |
|------|------|
| スプラッシュ / ログイン | Google ログインボタンのみ。初回のみ表示。 |
| ホーム (タイムライン) | 日付ごとのセクション、記録カード (写真・店名・メニュー・金額)。右下に FAB (カメラ)。下部ナビ: ホーム / 検索 / 統計 / 設定。 |
| カメラ | CameraX プレビュー。シャッター、ギャラリー選択、種別切替 (料理 / メニュー)。 |
| 記録編集 | 写真サムネイル列、店名 (候補チップ)、メニュー名 (候補チップ)、金額、評価、タグ、メモ、日時、食べログ URL 欄。上部に「保存」。 |
| 記録詳細 | 写真ページャ、各項目、地図サムネイル、編集 / 削除 / 共有 / ブログ投稿。 |
| 検索 | 検索窓 + フィルタチップ + 結果リスト。 |
| 店舗詳細 | 店舗情報、訪問回数、その店の記録一覧。 |
| 統計 | 月間支出、記録数、ジャンル比率、よく行く店 Top5。 |
| 設定 | アカウント、連携 (Sheets / ブログ)、通知、プライバシー (EXIF 除去)、エクスポート、ログアウト、アカウント削除。 |

**入力最小化の設計原則**
- 保存に必須なのは「写真 1 枚」または「メニュー名」のどちらか一方のみ。
- 店名・日時・位置は自動補完され、ユーザーは確認するだけ。
- 候補チップは常に「最も選ばれそうなもの」を左端に置く。

### 9.2 Web

| 画面 | 内容 |
|------|------|
| ログイン | Google ログイン。 |
| 一覧 | タイムライン / グリッド / カレンダーの 3 表示切替。 |
| 詳細 | 写真拡大、全項目表示、編集。 |
| 検索・フィルタ | 期間・タグ・店舗・評価。 |
| 統計 | 月次グラフ。 |
| 設定 | 連携設定、エクスポート。 |
| 公開ページ (P2) | 限定公開 URL。OGP 対応で SNS 共有可。 |

- Web からの新規作成も可能 (写真アップロード + 手入力)。位置補完は
  ブラウザ Geolocation または写真 EXIF を使用。

---

## 10. 権限・プライバシー・法務

| 項目 | 方針 |
|------|------|
| Android 権限 | `CAMERA` (必須)、`ACCESS_FINE_LOCATION` (任意)、`POST_NOTIFICATIONS` (任意)。ストレージは Photo Picker を使い権限不要にする。 |
| 権限説明 | 初回リクエスト前に「なぜ必要か」を画面で説明してから OS ダイアログを出す。 |
| 位置情報 | 記録時のみ取得。保存しない設定も提供。既定の公開範囲は非公開。 |
| EXIF | アップロード前に位置情報を除去する設定を既定 ON。 |
| データ所有 | ユーザーはいつでも全データをエクスポート・削除できる。 |
| 削除 | アカウント削除要求から 30 日以内に Firestore / Storage / 連携トークンを完全消去。 |
| 第三者提供 | しない。解析は匿名化された利用統計のみ。 |
| 外部 API 規約 | Google Maps Platform の利用規約 (キャッシュ制限・帰属表示) を遵守。食べログは §6.3 の前提を厳守。 |
| 店舗写真 | メニュー表の撮影は店舗により禁止されている場合がある旨をアプリ内で注意喚起。 |
| 必要書類 | プライバシーポリシー、利用規約、Play Console のデータセーフティ申告。 |

---

## 11. 分析・品質

**計測イベント** (Firebase Analytics)
- `record_created` (source, has_photo, has_location, autofilled_restaurant)
- `restaurant_suggestion_accepted` / `rejected`
- `tabelog_resolve_success` / `failure`
- `sheets_sync_completed`, `blog_published`
- `time_to_save_ms` (カメラ起動から保存完了までの秒数)

**主要 KPI**
- D7 / D30 継続率、週あたり記録数、記録 1 件あたりの所要時間、
  店舗名の自動補完採用率 (目標 70% 以上)。

**テスト**
- ユニット: ViewModel / UseCase / OCR 解析ロジック / スプレッドシート行変換。
- 結合: Firestore エミュレータ + Functions エミュレータ。
- UI: Compose UI Test で「撮影 → 保存」のハッピーパス。
- 手動: 圏外 → 復帰時の同期、権限拒否時の動作、大量データ (1,000 件) のスクロール。

---

## 12. 開発マイルストーン

| フェーズ | 内容 | 完了条件 |
|----------|------|----------|
| M0: 設計 | 本仕様の確定、Firebase プロジェクト作成、CI 整備 | 仕様レビュー完了 |
| M1: MVP | F-101〜F-106, F-111, F-112, F-201, F-204, F-301, F-302 | 自分で毎日使える |
| M2: 補完強化 | F-105 高度化, F-109, F-110, F-107, F-108, F-205, F-206 | 入力が 15 秒以内 |
| M3: Web | Web 閲覧一式 (F-201〜F-207) | PC から振り返れる |
| M4: 連携 | F-401, F-403, F-404, F-405 | シート / ブログへ出せる |
| M5: 拡張 | F-208, F-209, F-402, F-406, ウィジェット | — |

---

## 13. リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| 食べログのスクレイピングが規約違反 | 機能停止・法的リスク | §6.3 方針 A/B を既定とし、URL 保存 + Places 名寄せを主軸にする。規約確認を M2 の着手条件とする。 |
| Places API のコスト増 | 運用費 | 呼び出しをサーバ集約し、同一座標 (100m グリッド) の結果を 24 時間キャッシュ。セッショントークンを活用。 |
| 写真によるストレージ肥大 | 費用 | アップロード前リサイズ、サムネイル生成、原本保持はオプトイン。 |
| 入力が面倒で継続されない | 離脱 | 保存必須項目を 1 つに絞る。`time_to_save_ms` を継続監視。 |
| OCR 精度が低い | 期待外れ | あくまで「候補提示」に留め、手入力を常に一級市民として扱う。 |
| Firestore の集計・検索が弱い | 機能制約 | 統計は事前集計ドキュメントを Functions で更新。必要なら PostgreSQL へ移行。 |
| ブログ API の認証情報漏洩 | 重大 | Secret Manager 管理、クライアントへ返さない、失効時は再認証を促す。 |

---

## 14. 未決事項 (要確認)

1. **収益化**: 個人利用ツールか、公開サービスか。公開する場合、無料枠と有料機能の線引き (例: 写真枚数・自動同期・統計)。
2. **iOS 対応**: 将来対応するか。するなら Android は Kotlin Multiplatform を見据えた層分けにするか。
3. **食べログ連携の深さ**: URL 保存のみで足りるか、店舗詳細 (点数・ジャンル) まで欲しいか。後者なら規約確認が必須。
4. **ブログ連携先の優先順位**: はてなブログ / WordPress / note / Micropub のどれを最初に実装するか。
5. **スプレッドシートの位置づけ**: 出力先 (一方向) か、シート編集をアプリへ反映する双方向同期か。MVP は一方向を想定。
6. **共有・公開機能**: 他人に見せる需要があるか。あるなら公開 URL の粒度 (記録単位 / 月単位 / 全体)。
7. **ランチ以外**: 夕食・カフェも記録したくなる可能性。データモデルは `mealType` を足せば拡張可能にしておくか。
8. **写真の原本保持**: 端末ストレージとクラウド費用のトレードオフ。既定をどちらにするか。
