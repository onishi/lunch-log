# ランチログ 開発計画

- 最終更新: 2026-09-12
- 対象仕様: [SPEC.md](./SPEC.md) v0.4

## この文書の役割

| 文書 | 役割 | 書くこと |
|------|------|----------|
| `SPEC.md` | **仕様** | 何を作るか。機能・データモデル・外部連携・制約。 |
| `PLAN.md` | **計画** | どの順で作るか。タスク・進捗・完了条件。 |

運用ルール:

- タスクが終わったら `[ ]` を `[x]` にする。
- フェーズ名は SPEC §12 の M0〜M6 と対応する (Phase 1 = M1)。
- **仕様を変えたくなったら、まず SPEC.md を直す。** PLAN には書かない。
- 実装順やタスクの粒度は PLAN 側で自由に見直してよい (SPEC は変えない)。
- SPEC の機能 ID (F-xxx) と節番号 (§x.x) を各タスクに添え、対応を追えるようにする。

---

## 進め方の原則

1. **MVP を最優先で完成させる。** 最小限の機能で「毎日の記録が回る」状態に
   一度到達してから、機能を足す。
2. **縦に薄く通す。** 認証だけ完璧に作る、ではなく「撮って保存して一覧に出る」を
   最短で貫通させ、各層は最低限で置く。
3. **動かないものを積み上げない。** 各フェーズの終わりに必ず自分の端末で動かす。
4. **後回しにしたものは明示的に書き残す。** 忘れるためではなく、
   思い出すために「やらないことリスト」を置く。

### スケジュール感

見積もりは置かない (ひとり開発で締切もないため)。代わりに**順序と完了条件**を守る。
フェーズは飛ばさず、前のフェーズの完了条件を満たしてから次へ進む。

---

## MVP の定義

> **MVP の完了条件: 自分が 1 週間、他のアプリを使わずにこのアプリだけで
> 毎日のランチを記録できた状態。**

### MVP に入れるもの

| 機能 | SPEC | なぜ MVP に必要か |
|------|------|-------------------|
| Google ログイン | F-301 | データの持ち主を決められないと同期もバックアップもできない |
| 料理写真の撮影・添付 | F-101 | 記録の中心。これがないと単なるメモアプリ |
| メニュー名・店名の入力 | F-103, F-104 | 検索・振り返りの手がかり |
| 位置情報からの店舗候補 | F-105 | **入力を減らす**というプロダクトの核 (§1.2) |
| 日時の記録 | F-106 | 記録の並び順の基準 |
| 食事種別 | F-116 | 後から列を足すのが面倒なので最初から持つ (値は時刻推定のみ) |
| オフライン保存と同期 | F-111, F-302 | 店内は電波が悪い。落ちたら二度と使わなくなる |
| 一覧・詳細 | F-201, F-204 | 見返せないなら記録する意味がない |
| 編集・削除 | F-112 | 打ち間違いを直せないと苦痛 |

### MVP に入れないもの (やらないことリスト)

意識的に外す。**MVP 中にこれらへ寄り道しない。**

- メニュー写真 (F-102) — 料理写真だけで記録は成立する → Phase 2
- メニュー写真の OCR (F-110) → Phase 2
- 金額・評価・タグ・メモ (F-107, F-108) → Phase 2
- 食べログ URL (F-109) → Phase 2 (理由は下記)
- 検索・フィルタ (F-205, F-206) — 数十件なら目で探せる → Phase 2
- 写真原本の保持 (F-117, §6.7) — MVP は表示用のみ → Phase 3
- Web (§9.2) → Phase 3
- スプレッドシート・はてなブログ (F-401, F-403) → Phase 4
- 統計・地図・ウィジェット・通知 → Phase 5
- 監視 `monitor` (§6.6) → Phase 6。**M5 まで着手しない**
- ダークテーマの作り込み、アニメーション、アイコンの作り込み

> F-109 (食べログ URL の保存) は実装が軽いが、「撮る → 保存 → 見返す」の
> 核には寄与しないため Phase 2 に置いた。

---

## Phase 0 (M0): 足場づくり

**完了条件: 空のアプリが自分の端末で起動し、CI が通る。**

### 開発環境

- [ ] Android Studio の最新安定版で新規プロジェクト作成 (Empty Compose Activity)
- [ ] パッケージ名を決める (例: `app.lunchlog`)
- [ ] `minSdk = 29` / `targetSdk` は最新安定版 (SPEC §4)
- [ ] Gradle Version Catalog (`libs.versions.toml`) で依存を一元管理
- [ ] モジュール構成を決める (MVP は単一モジュールで開始。分割は必要になってから)

### Firebase

- [ ] Firebase プロジェクトを作成 (`lunch-log-dev` / `lunch-log-prod` の 2 つ)
- [ ] Android アプリを登録し `google-services.json` を配置 (**リポジトリに含めない**)
- [ ] Authentication で Google プロバイダを有効化
- [ ] Firestore を作成 (ネイティブモード / asia-northeast1)
- [ ] Cloud Storage を作成 (同リージョン)
- [ ] セキュリティルール初版を書く (SPEC §7.6 — 所有者のみ read/write)
- [ ] Firebase エミュレータをローカルで起動できるようにする

### CI とコード品質

- [ ] `.gitignore` (`google-services.json`, `local.properties`, `*.keystore` を除外)
- [ ] GitHub Actions: `ktlint` / `detekt` / `test` / `assembleDebug`
- [ ] Renovate か Dependabot で依存更新 (任意)

### ドキュメント

- [ ] `README.md` — セットアップ手順 (Firebase 設定の入れ方を含む)
- [ ] `CLAUDE.md` — コーディング規約、ディレクトリ構成、よく使うコマンド

---

## Phase 1 (M1): MVP

**完了条件: 1 週間、毎日このアプリだけでランチを記録できた。**

タスクは**この順に**進める。各ステップの終わりで必ず実機で動かす。

### 1-1. 認証 (F-301, F-302)

- [ ] Credential Manager + Firebase Auth で Google ログイン
- [ ] ログイン画面 (ボタン 1 つ) / 起動時のログイン状態復元
- [ ] ログアウト (設定画面は後回しでよい。デバッグメニューで可)
- [ ] `users/{uid}` ドキュメントの初回作成 (SPEC §7.1、`settings` は既定値)

### 1-2. データ層 (SPEC §7.2)

- [ ] Room の `RecordEntity` / `PhotoEntity` を定義 (SPEC §7.2 の一部だけ)
  - MVP で持つ項目: `id`, `eatenAt`, `mealType`, `dishName`, `restaurantName`,
    `location`, `photos[]`, `createdAt`, `updatedAt`, `deletedAt`
  - **Phase 2 以降の項目 (price, rating, tags, memo, restaurantId) も
    カラムだけ先に用意する** — 後からのマイグレーションを減らすため
- [ ] ULID 生成ユーティリティ
- [ ] DAO と `RecordRepository` (Room を Single Source of Truth にする)
- [ ] Room のマイグレーション方針を決める (開発中は破壊的再作成でよい)
- [ ] ユニットテスト: Repository の CRUD

### 1-3. 撮影と保存 (F-101, F-103, F-106, F-116)

- [ ] CameraX で撮影画面 (プレビュー + シャッターのみ)
- [ ] Photo Picker でギャラリーからも選べるようにする (権限不要)
- [ ] 撮影画像を端末内に保存し、長辺 2048px / JPEG 85 にリサイズ (SPEC §4)
- [ ] EXIF から撮影日時を取得 → `eatenAt` の既定値に (F-106)
- [ ] EXIF の GPS を除去してから保存 (SPEC §10。原本保持は Phase 3)
- [ ] 記録編集画面: 写真サムネイル / メニュー名 / 店名 / 日時 / 食事種別
- [ ] 食事種別は時刻から推定 (11〜15 時 = ランチ)。選択 UI は最小限 (F-116)
- [ ] 保存 → Room に書き込み → ホームへ戻る
- [ ] **ここで一度リリースビルドを自分の端末に入れ、写真を撮って保存できることを確認**

### 1-4. 一覧・詳細・編集・削除 (F-201, F-204, F-112)

- [ ] ホーム: 日付降順のタイムライン (Paging は不要。まず全件表示)
- [ ] 記録カード (写真サムネ / 店名 / メニュー名 / 時刻)
- [ ] 記録詳細画面
- [ ] 編集 (1-3 の画面を再利用)
- [ ] 削除 = `deletedAt` を立てる論理削除。一覧から除外 (ゴミ箱 UI は Phase 2)
- [ ] 空状態の表示 (「まだ記録がありません」)

### 1-5. 位置情報と店舗候補 (F-104, F-105, SPEC §6.1)

ここだけサーバ (Cloud Functions) が必要になる。**MVP 唯一のサーバ実装。**

- [ ] Cloud Functions プロジェクト作成 (TypeScript)
- [ ] `POST /v1/places/nearby` を実装 (SPEC §8)
  - Places API `searchNearby` を呼ぶ。API キーは Functions 側に隠す
  - 半径 150m → 取れなければ 300m (SPEC §6.1)
  - Firebase ID トークンで認証
- [ ] 同一座標 (100m グリッド) の結果を 24 時間キャッシュ (コスト対策)
- [ ] Android: 権限説明画面 → `ACCESS_FINE_LOCATION` 要求 (SPEC §10)
- [ ] FusedLocationProvider で現在地取得 (10 秒タイムアウト)
- [ ] 記録編集画面に店舗候補チップを表示。タップで店名確定
- [ ] **フォールバックを必ず実装** (SPEC §6.1)
  - 権限拒否 / 取得失敗 → 直近に訪れた店の履歴を候補に
  - 候補にない → 自由入力
- [ ] スコアリングは距離順のみ (履歴・営業時間の加点は Phase 2)

### 1-6. 同期 (F-111, F-302)

- [ ] Firestore への書き込み (`users/{uid}/records/{recordId}`)
- [ ] Cloud Storage への写真アップロード (表示用 + サムネイル)
- [ ] WorkManager で再試行付きアップロードキュー
- [ ] Firestore のオフライン永続化を有効化
- [ ] 起動時に Firestore → Room へ差分取り込み
- [ ] 同期状態を記録カードに小さく表示 (未同期マーク)
- [ ] **機内モードで記録 → 復帰して同期されることを実機で確認**

### 1-7. MVP 仕上げ

- [ ] 権限説明を OS ダイアログの前に出す (SPEC §10)
- [ ] エラー表示 (保存失敗・同期失敗をユーザーに見える形で)
- [ ] アプリアイコンと名前 (仮でよい)
- [ ] リリースビルドの署名設定
- [ ] 自分の端末に入れて 1 週間使う ← **これが完了条件**

### MVP 受け入れチェック

1 週間使ったあとに確認する。

- [ ] カメラ起動から保存まで **30 秒以内**でできる (目標 15 秒は Phase 2)
- [ ] 店舗候補から店名を選べた記録が半分以上ある
- [ ] 圏外で記録し、復帰後に同期された
- [ ] アプリを削除 → 再インストール → ログインでデータが戻る
- [ ] 1 週間でクラッシュ 0 件
- [ ] 「面倒で記録しなかった日」が 1 日以下

> ここで問題が出たら、**Phase 2 に進まず MVP を直す。**

---

## Phase 2 (M2): 入力体験の強化

**完了条件: 記録 1 件あたりの所要時間が中央値 15 秒以内 (SPEC §11.3。体感で判断)。**

- [ ] メニュー写真の撮影・添付 (F-102) — 種別 `menu` を付けて保存
- [ ] メニュー写真の OCR (F-110, SPEC §6.2)
  - [ ] ML Kit Text Recognition v2 (日本語) を組み込み
  - [ ] 価格行 (`¥?[0-9,]{3,5}円?`) からメニュー名と価格を抽出
  - [ ] 候補チップ表示 → タップで反映
- [ ] メニュー名のサジェスト (F-103) — 自分の過去入力 / 同店の他記録から
- [ ] 金額 (F-107)、評価・メモ・タグ (F-108)
- [ ] 食べログ URL の保存 (F-109, SPEC §6.3)
  - [ ] URL 検証と正規化 (https + tabelog.com、クエリ除去)
  - [ ] 共有インテント受信 (`ACTION_SEND` / `text/plain`)
  - [ ] 詳細画面に「食べログで見る ↗」リンク
- [ ] 店舗候補のスコアリング強化 (F-105) — 訪問履歴の加点、営業時間
- [ ] 店舗マスタ `restaurants` の導入 (SPEC §7.3) と `restaurantId` の紐付け
- [ ] 検索 (F-205) / フィルタ (F-206、食事種別を含む)
- [ ] ゴミ箱 UI (30 日以内の復元)
- [ ] 設定画面 (アカウント、食事種別の既定、EXIF)

---

## Phase 3 (M3): Web と写真原本

**完了条件: PC のブラウザから 1 ヶ月分を振り返れ、写真の原本を取り出せる。**

### 3-1. 写真原本の保持 (F-117, SPEC §6.7)

- [ ] Photo モデルに `originalPath` / `originalBytes` / `originalUploadedAt` を追加
- [ ] サムネ (320px) / 表示用 (2048px) / 原本 の 3 段構成に変更
- [ ] 原本は WorkManager で **Wi-Fi + 充電中**にアップロード (既定)
- [ ] アップロード完了まで端末内の元ファイルを消さない
- [ ] 設定: `keepOriginals` / `originalUploadPolicy` / 使用容量の表示
- [ ] 原本は EXIF 無加工で `users/{uid}/originals/` へ

### 3-2. Web (SPEC §9.2)

- [ ] Next.js (App Router) + Firebase Hosting の雛形
- [ ] **全ページログイン必須** (自分専用。公開ページは作らない — SPEC §1.3)
- [ ] タイムライン / グリッド / カレンダーの 3 表示
- [ ] 記録詳細 — 写真拡大、**原本表示への切替とダウンロード**
- [ ] 原本ダウンロード用の署名付き URL 発行 (有効期限 1 時間、SPEC §7.6)
- [ ] 検索・フィルタ (F-205, F-206)
- [ ] 店舗別まとめ (F-207)
- [ ] Web からの記録作成・編集

---

## Phase 4 (M4): 外部連携

**完了条件: 1 ヶ月分をシートに出力でき、気に入った店をブログに下書き投稿できた。**

### 4-1. Google スプレッドシート出力 (F-401, SPEC §6.4)

- [ ] `drive.file` スコープの増分認可
- [ ] リフレッシュトークンを Secret Manager に保管
- [ ] スプレッドシート自動生成 (シート名 `records`、A〜Q 列のヘッダ)
- [ ] 記録 ID をキーにした upsert (`values.update` / `values.append`)
- [ ] 出力対象の既定はランチのみ (設定で全種別)
- [ ] **R 列以降は触らない**ことをテストで担保
- [ ] 「シートを作り直す」操作

### 4-2. はてなブログ投稿 (F-403, SPEC §6.5)

- [ ] **先に公式ドキュメントで AtomPub とフォトライフの仕様を確認する**
      (SPEC §14.2 M-2。これが着手条件)
- [ ] はてな ID / API キー / ブログ ID の登録画面と疎通確認
- [ ] 認証情報を Secret Manager へ (クライアントには返さない)
- [ ] WSSE ヘッダ生成 (ブログとフォトライフで共通化)
- [ ] フォトライフへの画像アップロード (表示用リサイズ版のみ)
- [ ] アップロード結果を `photos[].hatenaFotolife` に保存し、再投稿時は再利用
- [ ] 記事テンプレート (Mustache 風) と設定画面での編集
- [ ] Atom XML 生成 → 下書き投稿 (`<app:draft>yes</app:draft>` 固定)
- [ ] `rel="edit"` を保存し、再投稿は `PUT` で更新 (記事を重複させない)
- [ ] **画像アップロード失敗時は記事投稿ごと中止**する
- [ ] 投稿単位の選択 (1 記録 / 1 日 / 1 ヶ月)

### 4-3. エクスポート

- [ ] Markdown / HTML 生成 (F-404)
- [ ] CSV / JSON エクスポート (F-405)
- [ ] アカウント削除 (SPEC §10 — フォトライフの画像が残る旨の明示を含む)

---

## Phase 5 (M5): 拡張

**完了条件なし。使っていて欲しくなったものから着手する。**

- [ ] 統計ダッシュボード (F-208) — 月間支出、訪問店舗数、食事種別ごとの集計
- [ ] 地図表示 (F-209)
- [ ] スプレッドシート自動出力 (F-402) — 日次バッチ
- [ ] ホーム画面ウィジェット / クイックタイル (F-114)
- [ ] リマインダー通知 (F-115)

---

## Phase 6 (M6): 監視 (`monitor`)

**M5 まで着手しない (SPEC §6.6)。** それまでは Android vitals と
Cloud Logging で足りるものとする。

- [ ] `monitor` の連携方式を決める (プロトコル / 認証 / データモデル) — SPEC §14.2 M-1
- [ ] 決めた内容を **SPEC §6.6 に反映**してから実装に入る
- [ ] 送信項目を §11.2 の候補から取捨選択
- [ ] Android / Web / Functions からの送信を実装
- [ ] アラート設定

---

## 横断タスク (各フェーズで継続)

- [ ] 新しい外部 API を叩く前に、必ず利用規約と上限を確認する
- [ ] Firestore のインデックスを追加したら SPEC §7.5 に追記する
- [ ] セキュリティルールを変えたらエミュレータでテストを書く
- [ ] GCP の予算アラートを確認する (Places API / Cloud Storage — SPEC §13)
- [ ] 手動テスト: 圏外 → 復帰、権限拒否、大量データ (1,000 件)、
      Wi-Fi 切断下での原本アップロード保留

---

## 判断を保留しているもの

着手時に決める。SPEC §14.2 と対応。

| # | 保留事項 | いつ決めるか |
|---|----------|--------------|
| M-1 | `monitor` の連携方式 | Phase 6 の着手時 |
| M-2 | はてなフォトライフ API の実仕様 | Phase 4-2 の着手前 (**必須**) |
| M-3 | 原本を保存する既定値 | Phase 3 で容量を実測してから |
| M-4 | 食事種別の粒度 (6 種で足りるか) | Phase 2 でタグを入れたあと |
| M-5 | カロリー・栄養素 | 未定 (現状は非目標) |
| M-6 | ソース公開 | 未定 |

---

## 進捗ログ

フェーズを終えたら 1 行書く。

| 日付 | できごと |
|------|----------|
| 2026-09-12 | SPEC.md v0.4 確定。PLAN.md 作成。Phase 0 着手前。 |
